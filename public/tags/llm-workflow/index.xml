<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>LLM Workflow on RockB</title>
    <link>https://baeseokjae.github.io/tags/llm-workflow/</link>
    <description>Recent content in LLM Workflow on RockB</description>
    <image>
      <title>RockB</title>
      <url>https://baeseokjae.github.io/images/og-default.png</url>
      <link>https://baeseokjae.github.io/images/og-default.png</link>
    </image>
    <generator>Hugo</generator>
    <language>en-us</language>
    <lastBuildDate>Sat, 18 Apr 2026 11:16:57 +0000</lastBuildDate>
    <atom:link href="https://baeseokjae.github.io/tags/llm-workflow/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>LLM Coding Workflow Guide 2026: How Top Developers Structure AI-Assisted Development</title>
      <link>https://baeseokjae.github.io/posts/llm-coding-workflow-guide-2026/</link>
      <pubDate>Sat, 18 Apr 2026 11:16:57 +0000</pubDate>
      <guid>https://baeseokjae.github.io/posts/llm-coding-workflow-guide-2026/</guid>
      <description>A 5-phase framework for structured AI-assisted coding in 2026: spec-driven planning, context packing, iterative implementation, quality gates, and tooling.</description>
      <content:encoded><![CDATA[<p>The most effective LLM coding workflow in 2026 follows five phases: spec-driven planning, context packing, iterative implementation, automated quality gates, and persistent tooling infrastructure. Developers who follow this structure report 25–39% productivity gains versus ad-hoc prompting, which leaves most of the value on the table.</p>
<hr>
<h2 id="the-state-of-ai-assisted-development-in-2026-the-adoption-productivity-paradox">The State of AI-Assisted Development in 2026: The Adoption-Productivity Paradox</h2>
<p>AI coding tools have reached near-universal adoption in 2026 — roughly 92% of developers use them in some part of their workflow, and 51% use them every day, according to DX Research. Yet a striking gap has opened between usage rates and actual productivity outcomes. The same research finds developers save an average of 3.6 hours per week — far less than early projections promised. Worse, 66% of developers say the biggest problem is AI code that <em>looks</em> correct but fails during testing, wiping out the time they thought they saved. The root cause is almost always workflow structure: developers are using LLMs as turbo-autocomplete rather than as a structured development partner. Teams that close the productivity gap have done one thing differently — they treat AI assistance as a phased process with explicit inputs and outputs at each stage, not a stream-of-consciousness chat session.</p>
<h3 id="why-most-developers-are-using-ai-wrong">Why Most Developers Are Using AI Wrong</h3>
<p>The pattern is familiar: a developer opens their IDE, starts typing a vague prompt like &ldquo;build a user authentication system,&rdquo; and spends the next two hours debugging hallucinated API calls. This is the &ldquo;prompt and pray&rdquo; workflow, and it&rsquo;s responsible for the 93% adoption / 10% productivity paradox documented by engineering leads in 2026. The fix is not a better model — it&rsquo;s a better process. Models like Claude Opus 4.6 (which leads SWE-bench Verified at ~80% real-world bug fixing) are capable enough; the bottleneck is the developer&rsquo;s ability to structure context, break work into verifiable chunks, and maintain quality gates at each step.</p>
<hr>
<h2 id="phase-1--spec-driven-planning-why-top-developers-write-the-spec-before-any-code">Phase 1 — Spec-Driven Planning: Why Top Developers Write the Spec Before Any Code</h2>
<p>Spec-driven development is the single highest-leverage change a developer can make to their AI coding workflow. It works by producing a written specification — requirements, design decisions, API contracts, and explicit constraints — before asking any LLM to generate code. When an agent starts implementation with a well-formed spec, it has a ground truth to reason against, which dramatically reduces hallucinated paths and contradictory decisions mid-implementation. Alex Oprescu&rsquo;s walkthrough of spec-driven development with Claude Code showed a complete storage layer migration completed in a single day — a task that would normally span a sprint — by front-loading all ambiguity resolution into a spec.md file before touching the codebase. The principle is simple: every minute spent clarifying requirements in a spec saves five minutes of debugging AI-generated code that didn&rsquo;t understand the goal.</p>
<h3 id="what-belongs-in-a-good-spec-file">What Belongs in a Good Spec File</h3>
<p>A spec used for AI-assisted development is not a traditional PRD. It should be dense, opinionated, and written as if briefing a new hire who is fast but needs guardrails. Include:</p>
<ul>
<li><strong>Goal:</strong> one sentence on what &ldquo;done&rdquo; looks like</li>
<li><strong>Constraints:</strong> things the AI must not do (e.g., &ldquo;do not modify the legacy auth middleware&rdquo;)</li>
<li><strong>Invariants:</strong> properties that must always hold (e.g., &ldquo;all database writes go through the repository layer&rdquo;)</li>
<li><strong>Acceptance criteria:</strong> specific, testable conditions</li>
<li><strong>Anti-patterns:</strong> examples of approaches to avoid, with brief reasons</li>
</ul>
<p>Save this as <code>spec.md</code> or <code>tasks/[feature].md</code> in your repo. In Claude Code, load it explicitly: <code>claude --context spec.md</code> or reference it in your CLAUDE.md.</p>
<h3 id="the-osmani-framework-5-pillars-of-a-production-llm-workflow">The Osmani Framework: 5 Pillars of a Production LLM Workflow</h3>
<p>Addy Osmani (Google Chrome team) published the most widely-shared LLM workflow article of 2026, distilling his team&rsquo;s practice into five pillars: <strong>specs → skills → MCPs → small iterative chunks → always review</strong>. The framework has become the de facto standard because it maps cleanly to the failure modes developers actually hit. Skipping specs leads to implementation drift. Skipping skills (reusable prompt templates) leads to inconsistent output quality. Skipping MCPs leaves the agent context-blind about your actual stack. Oversized chunks cause compounding errors. And skipping review is how subtle bugs ship to production.</p>
<hr>
<h2 id="phase-2--context-packing-how-to-give-your-llm-everything-it-needs-to-succeed">Phase 2 — Context Packing: How to Give Your LLM Everything It Needs to Succeed</h2>
<p>Context packing is the practice of deliberately loading relevant information into an LLM&rsquo;s working context before beginning a task — rather than relying on the model to infer it from the codebase alone. In 2026, this skill separates developers who consistently get good AI output from those who spend their sessions correcting misunderstandings. Effective context packing is a &ldquo;brain dump&rdquo; technique: before every significant coding session, you assemble the goals, relevant invariants, recent decisions, edge cases to handle, and examples of the style or patterns you want. For teams using Claude Code, this practice is operationalized through CLAUDE.md — a persistent, version-controlled file that Claude reads automatically at the start of every session. Teams that treat CLAUDE.md as a living infrastructure artifact (not a one-time setup) consistently report faster, more accurate AI output because the model enters each session already knowing the rules of their codebase.</p>
<h3 id="how-to-structure-your-claudemd">How to Structure Your CLAUDE.md</h3>
<p>CLAUDE.md is not documentation — it is machine-readable context. Keep it tightly scoped to what an AI needs to make correct decisions, not what a human needs to understand the project. A high-signal CLAUDE.md includes:</p>
<table>
  <thead>
      <tr>
          <th>Section</th>
          <th>What to Include</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Project overview</strong></td>
          <td>One paragraph: what the system does, what it doesn&rsquo;t do</td>
      </tr>
      <tr>
          <td><strong>Tech stack</strong></td>
          <td>Framework versions, key libraries, database engine</td>
      </tr>
      <tr>
          <td><strong>Code conventions</strong></td>
          <td>File naming, import order, error handling patterns</td>
      </tr>
      <tr>
          <td><strong>Do-not-touch</strong></td>
          <td>Files or modules to never modify without explicit instruction</td>
      </tr>
      <tr>
          <td><strong>Test commands</strong></td>
          <td>Exact commands to run tests, lint, and type-check</td>
      </tr>
      <tr>
          <td><strong>Common patterns</strong></td>
          <td>Example snippets for the patterns used most often</td>
      </tr>
      <tr>
          <td><strong>Open decisions</strong></td>
          <td>Outstanding architectural choices the AI should flag, not decide</td>
      </tr>
  </tbody>
</table>
<p>Run <code>/init</code> in Claude Code to generate a starting CLAUDE.md from your repo, then refine it over time. Treat changes to CLAUDE.md with the same code review discipline as production code.</p>
<h3 id="the-context-window-discipline-rule">The Context Window Discipline Rule</h3>
<p>Even the best-packed context can overflow. The working rule from high-performing teams in 2026: <strong>one ticket, one context window</strong>. Break projects into tasks that map to a single, completable unit of work. When context compaction happens mid-task, the model loses thread. If you find yourself needing to say &ldquo;remember earlier when we decided&hellip;&rdquo; — the task was too large. Switch to a new context and reload only what&rsquo;s relevant.</p>
<hr>
<h2 id="phase-3--iterative-implementation-small-chunks-focused-prompts-fast-loops">Phase 3 — Iterative Implementation: Small Chunks, Focused Prompts, Fast Loops</h2>
<p>Iterative implementation in an LLM coding workflow means breaking implementation into the smallest verifiable unit of work, executing it, verifying output, and then moving to the next unit — rather than generating large blocks of code and reviewing them at the end. A typical high-performing loop looks like this: write tests for a single function, ask the LLM to implement to pass those tests, run the tests, address failures immediately, commit, and move to the next unit. Each loop takes 5–15 minutes. This approach works because it keeps the LLM&rsquo;s attention focused on a concrete, testable target — and it catches errors before they compound. Teams that generate entire features in one prompt report up to 3x more debugging time than teams using iterative loops, because errors in early decisions cascade into dozens of dependent functions before anyone reviews the output.</p>
<h3 id="prompt-design-for-iterative-loops">Prompt Design for Iterative Loops</h3>
<p>The structure of your implementation prompt matters. Effective prompts in 2026 follow a three-part pattern:</p>
<ol>
<li><strong>Frame the unit:</strong> &ldquo;Implement the <code>UserRepository.findByEmail()</code> method in <code>src/repositories/user.ts</code>&rdquo;</li>
<li><strong>State the constraint:</strong> &ldquo;It must use the existing <code>db.query()</code> interface, not raw SQL&rdquo;</li>
<li><strong>Define done:</strong> &ldquo;All tests in <code>tests/user-repository.spec.ts</code> pass&rdquo;</li>
</ol>
<p>Avoid open-ended requests like &ldquo;add error handling to the auth module.&rdquo; The model does not know what &ldquo;done&rdquo; looks like and will either under-deliver or over-engineer. Give it a target it can reason against.</p>
<h3 id="when-to-use-autonomous-agents-vs-supervised-loops">When to Use Autonomous Agents vs. Supervised Loops</h3>
<table>
  <thead>
      <tr>
          <th>Scenario</th>
          <th>Recommended Mode</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Well-defined spec, low-risk module</td>
          <td>Autonomous agent (headless)</td>
      </tr>
      <tr>
          <td>Ambiguous requirements</td>
          <td>Supervised loop with review at each step</td>
      </tr>
      <tr>
          <td>Database migrations</td>
          <td>Supervised loop — always review SQL output</td>
      </tr>
      <tr>
          <td>Test generation</td>
          <td>Autonomous — low blast radius</td>
      </tr>
      <tr>
          <td>Security-sensitive code</td>
          <td>Supervised loop, additional human review</td>
      </tr>
      <tr>
          <td>Refactoring large files</td>
          <td>Small autonomous chunks (one function at a time)</td>
      </tr>
  </tbody>
</table>
<hr>
<h2 id="phase-4--quality-gates-testing-hooks-and-ai-on-ai-code-review">Phase 4 — Quality Gates: Testing, Hooks, and AI-on-AI Code Review</h2>
<p>Quality gates in an LLM coding workflow are automated checkpoints that validate AI-generated code before it enters the main codebase — catching the class of errors that cause 66% of developers to report lost time. The most robust quality gate architecture in 2026 combines three layers: (1) test-first development, where tests are written before AI generates implementation; (2) Claude Code hooks, which trigger lint, type-check, and test runs automatically after every edit; and (3) AI-on-AI review, where a second LLM context reviews the output of the first for correctness, security, and adherence to project conventions. This last technique — AI-on-AI review — emerged in 2026 as the highest-signal quality gate for catching subtle logical errors that humans miss on quick review, and that test suites don&rsquo;t cover because the error is in the design, not the execution.</p>
<h3 id="how-to-configure-claude-code-hooks-for-automated-quality">How to Configure Claude Code Hooks for Automated Quality</h3>
<p>Claude Code hooks execute shell commands in response to agent actions. A minimal production setup:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;hooks&#34;</span>: {
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;PostToolUse&#34;</span>: [
</span></span><span style="display:flex;"><span>      {
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;matcher&#34;</span>: <span style="color:#e6db74">&#34;Edit|Write&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;hooks&#34;</span>: [
</span></span><span style="display:flex;"><span>          { <span style="color:#f92672">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;command&#34;</span>, <span style="color:#f92672">&#34;command&#34;</span>: <span style="color:#e6db74">&#34;npm run lint --silent&#34;</span> },
</span></span><span style="display:flex;"><span>          { <span style="color:#f92672">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;command&#34;</span>, <span style="color:#f92672">&#34;command&#34;</span>: <span style="color:#e6db74">&#34;npm run typecheck --silent&#34;</span> }
</span></span><span style="display:flex;"><span>        ]
</span></span><span style="display:flex;"><span>      }
</span></span><span style="display:flex;"><span>    ],
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;Stop&#34;</span>: [
</span></span><span style="display:flex;"><span>      {
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;matcher&#34;</span>: <span style="color:#e6db74">&#34;.*&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;hooks&#34;</span>: [
</span></span><span style="display:flex;"><span>          { <span style="color:#f92672">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;command&#34;</span>, <span style="color:#f92672">&#34;command&#34;</span>: <span style="color:#e6db74">&#34;npm test --silent&#34;</span> }
</span></span><span style="display:flex;"><span>        ]
</span></span><span style="display:flex;"><span>      }
</span></span><span style="display:flex;"><span>    ]
</span></span><span style="display:flex;"><span>  }
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>This ensures lint and type errors surface immediately after each file change, before the agent moves to the next step — preventing cascading failures from undetected type mismatches.</p>
<h3 id="ai-on-ai-code-review-the-emerging-2026-pattern">AI-on-AI Code Review: The Emerging 2026 Pattern</h3>
<p>The technique: after an agent completes a task, open a fresh LLM context (no implementation history), load the diff, and ask: &ldquo;Review this code for correctness, security issues, and adherence to these conventions: [paste relevant CLAUDE.md sections].&rdquo; A fresh context has no attachment to the choices made during implementation and will flag inconsistencies that the generating agent rationalized away. Teams using this pattern report catching ~30% more subtle bugs before PR — particularly in edge-case handling and security-sensitive paths.</p>
<hr>
<h2 id="the-2026-tool-stack-claudemd-mcp-servers-and-choosing-your-ide">The 2026 Tool Stack: CLAUDE.md, MCP Servers, and Choosing Your IDE</h2>
<p>The 2026 AI-assisted development tool stack has consolidated around three infrastructure layers: a persistent context file (CLAUDE.md or equivalent), MCP (Model Context Protocol) servers for tool integration, and an IDE or agentic runtime that ties them together. MCP is now the standard connection layer between AI agents and external systems — databases, issue trackers, design tools, monitoring dashboards, and CI systems. In 2026, MCP has moved from experimental to production-grade: teams are connecting Claude Code to Notion for spec management, Linear for ticket context, Figma for design-to-code flows, and Postgres for live schema awareness. The shift matters because it eliminates the copy-paste loop that previously broke context — instead of manually pasting a database schema into every session, the agent queries it directly through MCP, always working from the current state of the system.</p>
<h3 id="ide--agent-comparison-where-each-tool-fits-in-2026">IDE + Agent Comparison: Where Each Tool Fits in 2026</h3>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Best For</th>
          <th>Weakness</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Claude Code (CLI)</strong></td>
          <td>Agentic, multi-file tasks; spec-driven workflows</td>
          <td>No visual UI; terminal-native</td>
      </tr>
      <tr>
          <td><strong>Cursor</strong></td>
          <td>Fast inline edits; strong autocomplete</td>
          <td>Weaker at multi-step agent tasks</td>
      </tr>
      <tr>
          <td><strong>GitHub Copilot Enterprise</strong></td>
          <td>Teams standardized on GitHub; PR review integration</td>
          <td>Limited agentic capability vs. Claude Code</td>
      </tr>
      <tr>
          <td><strong>Windsurf</strong></td>
          <td>Beginners; low-friction onboarding</td>
          <td>Less configurable than Claude Code</td>
      </tr>
      <tr>
          <td><strong>Zed + LLM plugins</strong></td>
          <td>Performance-focused developers; Rust ecosystem</td>
          <td>Smaller plugin ecosystem</td>
      </tr>
  </tbody>
</table>
<p>For pure agentic workflows — where the LLM autonomously plans and executes multi-step tasks — Claude Code with a well-maintained CLAUDE.md and MCP integrations is the highest-leverage setup in 2026.</p>
<h3 id="mcp-servers-worth-adding-in-2026">MCP Servers Worth Adding in 2026</h3>
<p>Start with the integrations that eliminate the most context-switching in your specific workflow:</p>
<table>
  <thead>
      <tr>
          <th>MCP Server</th>
          <th>What It Solves</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Postgres / SQLite</strong></td>
          <td>Agent reads live schema; no more pasting DDL</td>
      </tr>
      <tr>
          <td><strong>Linear / GitHub Issues</strong></td>
          <td>Agent reads ticket context directly</td>
      </tr>
      <tr>
          <td><strong>Figma</strong></td>
          <td>Design-to-code with component context</td>
      </tr>
      <tr>
          <td><strong>Notion</strong></td>
          <td>Spec files accessible without copy-paste</td>
      </tr>
      <tr>
          <td><strong>Sentry / Datadog</strong></td>
          <td>Agent reads live error context during debugging</td>
      </tr>
  </tbody>
</table>
<p>Add servers with <code>claude mcp add [server-name]</code> and verify permissions before connecting to production databases.</p>
<hr>
<h2 id="faq">FAQ</h2>
<p>The following questions capture the most common sticking points developers hit when adopting a structured LLM coding workflow. Each answer is written to be self-contained — you can apply it directly without reading the rest of the guide. The core insight across all five questions is the same: AI coding tools are powerful enough in 2026 that the bottleneck is almost never model capability. It&rsquo;s workflow structure. Developers who are frustrated with LLM output quality will typically find one of five root causes here: missing spec, under-packed context, oversized implementation chunks, absent quality gates, or mismatched tool choice for the task type. Fixing any one of these usually produces an immediate, noticeable improvement in output quality and time saved — fixing all five is what separates the 25–39% productivity gain cohort from the majority who report marginal improvement despite heavy AI tool usage.</p>
<h3 id="what-is-spec-driven-development-in-the-context-of-llm-coding-workflows">What is spec-driven development in the context of LLM coding workflows?</h3>
<p>Spec-driven development is the practice of writing an explicit requirements and design specification — covering goals, constraints, invariants, and acceptance criteria — before asking an LLM to generate any code. The spec gives the model a ground truth to reason against, reducing hallucinated implementation paths and mid-task direction changes. In practice, this means maintaining a <code>spec.md</code> or per-feature task file that the agent loads at session start.</p>
<h3 id="how-do-i-set-up-a-claudemd-for-my-project">How do I set up a CLAUDE.md for my project?</h3>
<p>Run <code>/init</code> in Claude Code to auto-generate a starting CLAUDE.md from your existing codebase. Then refine it manually: add your tech stack versions, code conventions, commands to run tests, modules to never touch, and common patterns. Treat it like production code — review changes, commit them, and update it when conventions evolve. A well-maintained CLAUDE.md is the single highest-ROI setup investment in a Claude Code workflow.</p>
<h3 id="what-are-mcp-servers-and-which-ones-should-i-add-first">What are MCP servers and which ones should I add first?</h3>
<p>MCP (Model Context Protocol) servers are connections that give an AI agent live access to external systems — databases, issue trackers, design tools, monitoring platforms. Instead of manually pasting database schemas or ticket descriptions, the agent queries them directly. In 2026, start with the MCP server for your database (Postgres or SQLite) and your issue tracker (Linear or GitHub Issues) — these eliminate the two most common context gaps that cause LLM coding errors.</p>
<h3 id="what-is-ai-on-ai-code-review">What is AI-on-AI code review?</h3>
<p>AI-on-AI code review is the practice of using a second, fresh LLM context to review the code generated by a first context. The reviewing context loads only the diff and your project conventions — it has no attachment to the implementation decisions made during generation. This catches logical errors and security issues that the generating agent rationalized away, and that quick human review often misses. Teams using this pattern report catching ~30% more bugs before PR.</p>
<h3 id="how-do-i-know-when-to-use-autonomous-agents-vs-supervised-implementation-loops">How do I know when to use autonomous agents vs. supervised implementation loops?</h3>
<p>Use autonomous agents for well-defined, low-risk tasks with clear specs and strong test coverage — test generation, documentation, and isolated refactors. Use supervised loops (review output at each step) for anything touching databases, security-sensitive code, ambiguous requirements, or large files. A practical heuristic: if you&rsquo;d require a senior engineer to review a human implementation, use a supervised loop for the AI equivalent. The blast radius of a mistake is the deciding factor.</p>
]]></content:encoded>
    </item>
  </channel>
</rss>
