<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>1password on RockB</title><link>https://baeseokjae.github.io/tags/1password/</link><description>Recent content in 1password on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Sun, 26 Apr 2026 00:04:05 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/1password/index.xml" rel="self" type="application/rss+xml"/><item><title>1Password Unified Access for AI Agents: Developer Security Guide</title><link>https://baeseokjae.github.io/posts/1password-unified-access-ai-agents-2026/</link><pubDate>Sun, 26 Apr 2026 00:04:05 +0000</pubDate><guid>https://baeseokjae.github.io/posts/1password-unified-access-ai-agents-2026/</guid><description>How to use 1Password Unified Access to securely inject credentials into AI agents at runtime — no hardcoded secrets, full audit trail.</description><content:encoded><![CDATA[<p>1Password Unified Access is a secrets management platform that lets you discover, secure, and audit credentials across human users, machine identities, and AI agents from a single control plane — launched as generally available on March 17, 2026, with partners Anthropic, Cursor, GitHub, Perplexity, and Vercel.</p>
<h2 id="what-is-1password-unified-access-and-why-ai-agents-need-it-now">What Is 1Password Unified Access (and Why AI Agents Need It Now)</h2>
<p>1Password Unified Access is an enterprise identity platform that extends 1Password&rsquo;s credential management beyond human users to cover machine identities and AI agents. Launched on March 17, 2026, as generally available, Unified Access Pro introduces three operational pillars — Discover, Secure, and Audit — that give security and engineering teams a single pane of glass for managing every credential type in an organization. Unlike traditional password managers or standalone secrets managers, Unified Access is purpose-built for the era of autonomous AI agents, where software systems independently authenticate to APIs, databases, and third-party services without human involvement at each step. 1Password already secures 1.3 billion human and machine credentials across 180,000 businesses; Unified Access extends that trust model to agentic workloads. The core value proposition for developers: agents receive credentials at task runtime via SDK calls instead of reading static API keys from disk or environment files. This means a leaked agent configuration file exposes zero usable secrets.</p>
<h3 id="why-runtime-injection-changes-the-security-model">Why Runtime Injection Changes the Security Model</h3>
<p>Traditional secrets management passes credentials once at startup via environment variables or config files. Runtime injection means your agent calls <code>secrets.resolve()</code> immediately before each API operation, receiving a short-lived credential that was never stored in the agent process environment. If an attacker compromises agent memory mid-execution, they capture a credential that expires within minutes rather than a static key valid for months.</p>
<h2 id="the-non-human-identity-crisis-why-ai-agents-are-the-new-attack-surface">The Non-Human Identity Crisis: Why AI Agents Are the New Attack Surface</h2>
<p>Non-human identities — service accounts, API keys, machine certificates, and AI agent credentials — now outnumber human users 100-to-1 in enterprise environments. Gartner projects that by 2026, 30% of enterprises will rely on AI agents that act independently, executing transactions and completing tasks without human approval at each step. This scale creates a credential sprawl problem that most organizations have not yet confronted: each agent needs its own scoped identity, and those identities must be provisioned, rotated, and deprovisioned as agents spin up and down at runtime. GitGuardian&rsquo;s 2025 report found 24 million leaked credentials on GitHub, with 70% of secrets leaked in 2022 still valid today. AI agents dramatically expand the attack surface because they are designed to take autonomous actions using those credentials. BeyondTrust frames overprivileged AI agents as &ldquo;the new insider threat&rdquo; — an agent with standing access to your production database, S3 bucket, and Stripe API is one prompt injection away from becoming a data exfiltration vector. The answer is treating each agent as a first-class identity with scoped authentication, just-in-time access, and ephemeral credentials rather than persistent API keys.</p>
<h3 id="what-first-class-ai-agent-identity-means-in-practice">What &ldquo;First-Class AI Agent Identity&rdquo; Means in Practice</h3>
<p>A first-class identity means the agent has its own service account, its own audit trail, and its own access policy — not a shared service account borrowed from a human user or a CI pipeline. 1Password Unified Access provisions a dedicated vault per agent, scoped to only the credentials that agent requires. When the agent is decommissioned, the vault and its service account are removed, leaving no orphaned credentials.</p>
<h2 id="core-architecture--discover-secure-audit">Core Architecture — Discover, Secure, Audit</h2>
<p>1Password Unified Access Pro is built on three interdependent pillars that cover the complete credential lifecycle for AI agents. <strong>Discover</strong> continuously scans your infrastructure for exposed credentials — SSH keys, <code>.env</code> files, hardcoded tokens in repositories, and active AI agent sessions — giving you a live inventory of every credential and every identity accessing it. <strong>Secure</strong> centralizes those credentials into 1Password vaults, replaces hardcoded secrets with dynamic references, and enforces access policies that restrict each agent to exactly the credentials it needs. <strong>Audit</strong> maintains a unified access trail across every human, machine, and agent identity: who accessed what credential, when, and from which agent execution context. This Discover → Secure → Audit loop is the operational model for zero-trust agentic infrastructure. For developers, the practical entry point is Secure: create a vault, add your secrets, create a service account with read-only access scoped to that vault, and use the service account token to authenticate SDK calls from your agent. Discover and Audit run continuously in the background, surfacing anomalies and compliance evidence without developer intervention.</p>
<h3 id="how-unified-access-differs-from-1password-business">How Unified Access Differs from 1Password Business</h3>
<p>1Password Business manages human credentials with team sharing. Unified Access adds a machine identity layer: service accounts for agents, SDK-based resolution (not human UI flows), and the Discover scanner that identifies agentic credential exposure patterns. If you already use 1Password Business, Unified Access is an add-on tier rather than a separate product.</p>
<h2 id="developer-quickstart-integrating-1password-sdks-with-ai-agents-python-js-go">Developer Quickstart: Integrating 1Password SDKs with AI Agents (Python, JS, Go)</h2>
<p>The 1Password SDK enables runtime credential injection in three languages — Python, JavaScript/TypeScript, and Go — with a consistent pattern: authenticate with a service account token, then call <code>secrets.resolve()</code> with a secret reference URI to retrieve a credential immediately before use. The secret reference URI format is <code>op://{vault}/{item}/{field}</code>, where vault and item names match your 1Password vault structure. Here is the Python pattern for an AI agent that needs to call the Anthropic API without storing the key:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> asyncio
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> onepassword.client <span style="color:#f92672">import</span> Client
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">async</span> <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">get_anthropic_key</span>():
</span></span><span style="display:flex;"><span>    client <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> Client<span style="color:#f92672">.</span>authenticate(
</span></span><span style="display:flex;"><span>        auth<span style="color:#f92672">=</span>os<span style="color:#f92672">.</span>environ[<span style="color:#e6db74">&#34;OP_SERVICE_ACCOUNT_TOKEN&#34;</span>],
</span></span><span style="display:flex;"><span>        integration_name<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;my-ai-agent&#34;</span>,
</span></span><span style="display:flex;"><span>        integration_version<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;1.0.0&#34;</span>
</span></span><span style="display:flex;"><span>    )
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> <span style="color:#66d9ef">await</span> client<span style="color:#f92672">.</span>secrets<span style="color:#f92672">.</span>resolve(<span style="color:#e6db74">&#34;op://ai-agents/anthropic/api-key&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># The key is fetched at task time, not at startup</span>
</span></span><span style="display:flex;"><span>api_key <span style="color:#f92672">=</span> asyncio<span style="color:#f92672">.</span>run(get_anthropic_key())
</span></span></code></pre></div><p>The JavaScript/TypeScript equivalent uses the same URI format:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-typescript" data-lang="typescript"><span style="display:flex;"><span><span style="color:#66d9ef">import</span> { <span style="color:#a6e22e">createClient</span> } <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#34;@1password/sdk&#34;</span>;
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">client</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">createClient</span>({
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">auth</span>: <span style="color:#66d9ef">process.env.OP_SERVICE_ACCOUNT_TOKEN</span><span style="color:#f92672">!</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">integrationName</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;my-ai-agent&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">integrationVersion</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;1.0.0&#34;</span>,
</span></span><span style="display:flex;"><span>});
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">apiKey</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">client</span>.<span style="color:#a6e22e">secrets</span>.<span style="color:#a6e22e">resolve</span>(<span style="color:#e6db74">&#34;op://ai-agents/anthropic/api-key&#34;</span>);
</span></span></code></pre></div><p>And in Go:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-go" data-lang="go"><span style="display:flex;"><span><span style="color:#a6e22e">client</span>, <span style="color:#a6e22e">err</span> <span style="color:#f92672">:=</span> <span style="color:#a6e22e">op</span>.<span style="color:#a6e22e">NewClient</span>(<span style="color:#a6e22e">ctx</span>,
</span></span><span style="display:flex;"><span>    <span style="color:#a6e22e">op</span>.<span style="color:#a6e22e">WithServiceAccountToken</span>(<span style="color:#a6e22e">os</span>.<span style="color:#a6e22e">Getenv</span>(<span style="color:#e6db74">&#34;OP_SERVICE_ACCOUNT_TOKEN&#34;</span>)),
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">secret</span>, <span style="color:#a6e22e">err</span> <span style="color:#f92672">:=</span> <span style="color:#a6e22e">client</span>.<span style="color:#a6e22e">Secrets</span>.<span style="color:#a6e22e">Resolve</span>(<span style="color:#a6e22e">ctx</span>, <span style="color:#e6db74">&#34;op://ai-agents/anthropic/api-key&#34;</span>)
</span></span></code></pre></div><p>The only value you set in the environment is <code>OP_SERVICE_ACCOUNT_TOKEN</code>, which is itself a 1Password-managed credential. You can inject it via <code>op run</code> at agent launch so even the token never touches your codebase.</p>
<h3 id="creating-the-service-account-and-vault">Creating the Service Account and Vault</h3>
<p>In 1Password, create a dedicated vault named for your agent (<code>ai-agents</code> or more specific like <code>flight-booking-agent</code>). Add your API keys and credentials as items in that vault. Then create a service account with read-only access scoped to that vault only — the service account cannot read items in other vaults, cannot create or delete credentials, and has no UI access. Copy the service account token and store it as an environment variable in your deployment system (not in your code repository).</p>
<h2 id="mcp-server-integration--giving-claude-and-cursor-access-to-your-vault">MCP Server Integration — Giving Claude and Cursor Access to Your Vault</h2>
<p>The Model Context Protocol (MCP) integration lets Claude, Cursor, and other MCP-compatible AI assistants pull credentials from your 1Password vault at runtime, without embedding tokens in configuration files. This is the mechanism that prevents credential exposure in MCP server config files — a common attack vector as developers configure local AI development environments. To configure the 1Password MCP server, add it to your MCP client configuration using the <code>op run</code> command as a transport wrapper, which injects secrets at launch time without touching config files. The <code>op run</code> command reads secret reference URIs from your command environment and resolves them against your vault before spawning the process. For Claude Desktop, the configuration looks like:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;mcpServers&#34;</span>: {
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;1password&#34;</span>: {
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;command&#34;</span>: <span style="color:#e6db74">&#34;op&#34;</span>,
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;args&#34;</span>: [<span style="color:#e6db74">&#34;run&#34;</span>, <span style="color:#e6db74">&#34;--&#34;</span>, <span style="color:#e6db74">&#34;1password-mcp-server&#34;</span>],
</span></span><span style="display:flex;"><span>      <span style="color:#f92672">&#34;env&#34;</span>: {
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;OP_SERVICE_ACCOUNT_TOKEN&#34;</span>: <span style="color:#e6db74">&#34;op://dev-tools/mcp-service-account/token&#34;</span>
</span></span><span style="display:flex;"><span>      }
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>  }
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>This means the service account token itself is resolved from 1Password at startup, so the config file contains only a reference, not a credential. When Claude or Cursor needs to call an external API, the MCP server resolves the credential from the vault and passes it to the tool call. The Unified Access audit trail records each credential access with the MCP session context, giving you visibility into which AI assistant accessed which credential and when.</p>
<h3 id="setting-up-op-run-for-existing-agent-workflows">Setting Up op run for Existing Agent Workflows</h3>
<p>If you have an existing agent that reads credentials from environment variables, you can migrate to runtime injection incrementally using <code>op run</code>. Replace <code>ANTHROPIC_API_KEY=sk-...</code> in your <code>.env</code> with <code>ANTHROPIC_API_KEY=op://ai-agents/anthropic/api-key</code>, then launch your agent with <code>op run --env-file=.env -- python agent.py</code>. The <code>op run</code> command resolves the references and injects real values as environment variables before your process starts — zero code changes required.</p>
<h2 id="service-accounts-and-runtime-credential-delivery-step-by-step-setup">Service Accounts and Runtime Credential Delivery: Step-by-Step Setup</h2>
<p>Service accounts are the identity primitive for AI agents in 1Password Unified Access. Each service account has a scoped token, an access policy, and an audit trail independent of any human user. Setting up runtime credential delivery takes five steps. First, create a vault in 1Password for your agent&rsquo;s credentials — one vault per agent or agent class, not a shared vault. Second, add your secrets as items in that vault: API keys, database passwords, OAuth client secrets. Third, create a service account in 1Password developer settings, assign it read-only access to your new vault, and copy the generated token. Fourth, store the service account token in your deployment environment as <code>OP_SERVICE_ACCOUNT_TOKEN</code> — in Kubernetes as a sealed secret, in GitHub Actions as an encrypted secret, in Vercel as an environment variable. Fifth, update your agent code to call <code>secrets.resolve()</code> with the <code>op://vault/item/field</code> URI pattern immediately before each credential use. The service account token is the only credential that leaves 1Password infrastructure; all other secrets remain in the vault and are resolved on demand. When you decommission the agent, delete the service account to immediately revoke all its access — no credential rotation required.</p>
<h3 id="token-rotation-and-expiry">Token Rotation and Expiry</h3>
<p>Service account tokens support configurable expiry. For short-lived agent tasks (a single flight booking, an expense report submission), you can generate a token with a 24-hour TTL so it expires automatically after the task window. For persistent agents, configure automatic rotation via the 1Password API and update the token in your deployment secrets store on each rotation event.</p>
<h2 id="1password-vs-hashicorp-vault-vs-doppler-vs-akeyless-for-ai-agent-workloads">1Password vs HashiCorp Vault vs Doppler vs Akeyless for AI Agent Workloads</h2>
<p>Each secrets management platform has a different fit for AI agent workloads. 1Password Unified Access is the only platform that combines consumer-grade UX, developer secrets management, and AI agent identity in one product — with the Discover scanner and unified audit trail covering all three identity types.</p>
<table>
  <thead>
      <tr>
          <th>Platform</th>
          <th>AI Agent Support</th>
          <th>Operational Overhead</th>
          <th>Developer UX</th>
          <th>Agentic Audit Trail</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>1Password Unified Access</strong></td>
          <td>Native (SDK, MCP, service accounts)</td>
          <td>Low (SaaS)</td>
          <td>Excellent</td>
          <td>Yes — unified across human + agent</td>
      </tr>
      <tr>
          <td><strong>HashiCorp Vault</strong></td>
          <td>Via dynamic secrets + AppRole</td>
          <td>High (self-hosted)</td>
          <td>Steep learning curve</td>
          <td>Yes, but siloed</td>
      </tr>
      <tr>
          <td><strong>Doppler</strong></td>
          <td>Env injection only</td>
          <td>Low (SaaS)</td>
          <td>Strong</td>
          <td>No agent-specific audit</td>
      </tr>
      <tr>
          <td><strong>Akeyless</strong></td>
          <td>Via API + zero-knowledge arch</td>
          <td>Medium</td>
          <td>Good</td>
          <td>Partial</td>
      </tr>
  </tbody>
</table>
<p>HashiCorp Vault offers the most powerful secret engine capabilities — dynamic secrets, PKI, database credential rotation — but requires significant operational investment and a dedicated secrets infrastructure team. For a startup or mid-size company where developers manage their own tooling, the operational overhead often exceeds the security benefit. Doppler excels at developer experience for environment variable management but has no concept of AI agent identity or agentic audit trails. Akeyless brings a cloud-native zero-knowledge architecture that appeals to regulated industries, but its AI agent integration story lags behind 1Password&rsquo;s SDK and MCP support. The practical recommendation: if your organization already uses 1Password Business, Unified Access is the lowest-friction path to agentic secrets management. If you have existing investment in HashiCorp Vault, the AppRole auth method plus Vault Agent sidecar can cover AI agent credential delivery, but plan for significantly more infrastructure work.</p>
<h2 id="best-practices-for-governing-ai-agent-credentials-at-scale">Best Practices for Governing AI Agent Credentials at Scale</h2>
<p>Governing AI agent credentials at scale requires treating agents as first-class identities with the same lifecycle management as human employees. Four practices separate mature agentic security postures from credential sprawl. First, one vault per agent class — not per agent instance, but per behavioral class. A fleet of flight-booking agents shares one vault because they need the same credentials; a separate expense-reporting agent class gets its own vault with only the finance API credentials it needs. Second, minimum viable permissions: the service account for each agent class should have read access to exactly the items in its vault and nothing else. 1Password&rsquo;s vault access model makes this straightforward to enforce without complex policy expressions. Third, automated deprovisioning: tie service account lifecycle to your agent deployment pipeline. When a Kubernetes deployment is torn down, a post-hook calls the 1Password API to delete the service account. Fourth, alert on anomalous access patterns: the Unified Access audit trail surfaces deviations from baseline — an agent that normally accesses two credentials suddenly accessing ten is a signal worth investigating. Connect the 1Password audit log to your SIEM for automated alerting.</p>
<h3 id="human-in-the-loop-approval-for-sensitive-credentials">Human-in-the-Loop Approval for Sensitive Credentials</h3>
<p>For credentials that grant write access to production systems — database write credentials, payment API keys, email sending credentials — consider a human-in-the-loop approval flow where the agent requests access and a human approves before the credential is injected. 1Password&rsquo;s API supports building this pattern: the agent calls a webhook, a human approves in a Slack message, and the approval triggers a time-limited service account token. The credential is injected only after approval and expires when the task completes.</p>
<h2 id="roadmap-whats-coming-to-unified-access-in-2026">Roadmap: What&rsquo;s Coming to Unified Access in 2026</h2>
<p>1Password&rsquo;s 2026 roadmap for Unified Access focuses on three capability areas based on public announcements and the patterns established at the March 2026 GA launch. Automated discovery is expanding from static scanning to real-time credential exposure detection — rather than scheduled scans, Discover will emit alerts within minutes of a new <code>.env</code> file appearing in a repository or a new API key being hardcoded in a deployment configuration. Cross-platform agent identity federation is in development, targeting OAuth 2.0-based agent authentication flows where an AI agent can authenticate to external services using a 1Password-managed identity rather than a static API key — aligning with the emerging standard for agentic OAuth grants being developed in collaboration with launch partners Anthropic and GitHub. Finally, consent and approval workflows for high-privilege operations are on the roadmap: structured flows where an AI agent requests access to a sensitive credential, the request surfaces to a human approver via Slack or email, and approval generates a time-limited token scoped to the specific operation. This brings the human-in-the-loop model from manual implementation patterns to a first-class platform feature.</p>
<hr>
<h2 id="faq">FAQ</h2>
<p>The most common questions about 1Password Unified Access for AI agents center on three topics: how it differs from 1Password Business, which AI frameworks it supports, and how to handle credential rotation in autonomous agent workflows. Unified Access Pro — generally available since March 17, 2026 — is designed for engineering and security teams deploying autonomous agents at scale, not just individual developers managing passwords. The platform&rsquo;s service account model, SDK-based resolution, and Discover scanner address the specific failure modes of agentic systems: credential sprawl across agent configuration files, lack of visibility into which agent accessed which credential, and inability to revoke access quickly when an agent is decommissioned. The answers below address the practical implementation questions that come up most often when migrating from static API keys to runtime credential injection.</p>
<h3 id="how-does-1password-unified-access-differ-from-1password-business">How does 1Password Unified Access differ from 1Password Business?</h3>
<p>1Password Business manages human user credentials with team sharing and a web or desktop UI. Unified Access adds a machine identity layer with service accounts for AI agents, SDK-based credential resolution (not human UI flows), and the Discover scanner that identifies exposed secrets and agentic credential patterns. If you already use 1Password Business, Unified Access is available as an add-on tier.</p>
<h3 id="can-i-use-1password-unified-access-with-any-ai-framework-or-only-anthropic-claude">Can I use 1Password Unified Access with any AI framework or only Anthropic Claude?</h3>
<p>The 1Password SDK (Python, JavaScript/TypeScript, Go) works with any AI agent framework — LangChain, AutoGen, custom agent loops, or raw API calls. The MCP integration works with any MCP-compatible AI assistant, including Claude, Cursor, and others. The Anthropic partnership at launch means tighter integration for Claude-specific tooling, but the underlying SDK has no framework dependency.</p>
<h3 id="what-is-the-op-run-command-and-when-should-i-use-it">What is the <code>op run</code> command and when should I use it?</h3>
<p><code>op run</code> is a CLI wrapper that resolves 1Password secret references in environment variables before spawning a subprocess. Use it when you want to migrate an existing agent to runtime credential injection without changing code: replace credential values in your <code>.env</code> file with <code>op://vault/item/field</code> references and launch with <code>op run --env-file=.env -- your-agent-command</code>. For new agents, prefer the SDK for more granular control over when credentials are resolved.</p>
<h3 id="how-do-i-handle-credential-rotation-for-ai-agents">How do I handle credential rotation for AI agents?</h3>
<p>With runtime injection via the SDK, rotation is transparent: update the credential value in your 1Password vault, and the next <code>secrets.resolve()</code> call returns the new value. There is no restart or redeployment required unless the agent caches credentials. The recommendation is never to cache credentials in agent memory; resolve them immediately before each use to ensure you always get the current value.</p>
<h3 id="is-the-service-account-token-itself-secure-if-someone-reads-the-environment-of-the-running-process">Is the service account token itself secure if someone reads the environment of the running process?</h3>
<p>Capturing the service account token from process environment gives an attacker the ability to resolve credentials from the scoped vault for as long as the token is valid. Mitigate this with short token TTLs (24 hours for task-scoped agents), vault access scoped to only necessary items, and 1Password&rsquo;s anomaly detection alerting on unusual resolution patterns. For highest security, use <code>op run</code> so even the token is injected from a 1Password reference and is not a static value in any configuration file.</p>
]]></content:encoded></item></channel></rss>