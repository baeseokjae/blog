<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Developer Guide on RockB</title><link>https://baeseokjae.github.io/tags/developer-guide/</link><description>Recent content in Developer Guide on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Thu, 30 Apr 2026 21:09:48 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/developer-guide/index.xml" rel="self" type="application/rss+xml"/><item><title>Llama 4 Scout Developer Guide 2026: 10M Token Context Window for Full Codebase Analysis</title><link>https://baeseokjae.github.io/posts/llama-4-scout-guide-2026/</link><pubDate>Thu, 30 Apr 2026 21:09:48 +0000</pubDate><guid>https://baeseokjae.github.io/posts/llama-4-scout-guide-2026/</guid><description>Practical guide to Llama 4 Scout&amp;#39;s 10M token context window: API setup, full codebase loading, local deployment with Ollama and vLLM, and real pricing comparisons.</description><content:encoded><![CDATA[<p>Llama 4 Scout is Meta&rsquo;s open-weight model with a 10 million token context window — the largest of any open-weight model released in 2026. At roughly 4 tokens per line of code, that covers approximately 2.5 million lines of code in a single prompt. In practice this means you can load an entire mid-size production repository — including tests, docs, and config — without chunking, vector databases, or retrieval pipelines.</p>
<h2 id="what-is-llama-4-scout-model-specs-and-architecture">What Is Llama 4 Scout? Model Specs and Architecture</h2>
<p>Llama 4 Scout is a Mixture-of-Experts (MoE) model with 109 billion total parameters and 17 billion active parameters per forward pass, distributed across 16 experts. Meta trained it on 40 trillion tokens of data — roughly 3× the dataset used for Llama 3. The model fits on a single NVIDIA H100 80GB GPU using INT4 AWQ quantization (approximately 55GB), which makes it unusual for its class: a 10M context model that doesn&rsquo;t require a server cluster. The architecture uses iRoPE (interleaved Rotary Position Embeddings with NoPE layers), which applies temperature scaling at specific transformer layers to maintain coherence at extreme context lengths. This is not a marketing claim — Scout maintains greater than 99% accuracy at 10 million tokens in NIAH (Needle in a Haystack) long-context retrieval tests, meaning it reliably finds specific information buried anywhere in a 10M token prompt. For developers, the key architectural implication is that context degradation — the &ldquo;lost in the middle&rdquo; problem that plagues other models — is substantially reduced. Upstream on Hugging Face, the full model is <code>meta-llama/Llama-4-Scout-17B-16E-Instruct</code>.</p>
<h2 id="why-10m-tokens-is-a-qualitative-shift-for-developers">Why 10M Tokens Is a Qualitative Shift for Developers</h2>
<p>Ten million tokens is not just a bigger number — it changes the entire architecture of AI coding tools. Before Scout, any serious codebase analysis required building a RAG pipeline: chunking files, generating embeddings, storing vectors, writing retrieval logic, tuning similarity thresholds, and handling chunk-boundary artifacts where a function gets split across two retrieved chunks. With Scout&rsquo;s 10M context window, that entire layer disappears. Load the repo directly, ask questions, get answers that span across the entire codebase simultaneously.</p>
<p>At $0.08 per million input tokens (the cheapest available provider), loading a 2M-token codebase costs $0.16 per analysis run. Compare this to the engineering time to build and maintain a RAG pipeline — even one day of engineering time is worth hundreds of thousands of RAG queries at Scout&rsquo;s pricing. Beyond cost, there&rsquo;s correctness: RAG systems miss cross-file relationships unless you&rsquo;ve designed explicit cross-reference retrieval. Scout sees everything at once. I&rsquo;ve found that for security audits, refactoring analysis, and onboarding large codebases, the &ldquo;just load it all&rdquo; approach catches issues that chunked retrieval consistently misses — especially when a bug involves three files that wouldn&rsquo;t normally be retrieved together.</p>
<h3 id="the-math-on-codebase-coverage">The Math on Codebase Coverage</h3>
<p>At 4 tokens per line (a reasonable average for mixed Python/TypeScript/Go), 10M tokens covers approximately 2.5 million lines. A typical mid-size SaaS backend runs 200,000–500,000 lines of code. You can fit 5–12 of these simultaneously. The Linux kernel is 36 million lines — too large for one pass, but any single subsystem fits comfortably. For most teams, the entire active codebase fits in a single context with room to spare for full test suites and documentation.</p>
<h2 id="llama-4-scout-vs-gpt-41-claude-and-gemini-context-window-comparison">Llama 4 Scout vs. GPT-4.1, Claude, and Gemini: Context Window Comparison</h2>
<p>Llama 4 Scout occupies a unique position in the context window landscape: it matches Gemini 3 Pro&rsquo;s 10M token maximum but costs 150× less per million input tokens. Here&rsquo;s the current state of large-context models:</p>
<table>
  <thead>
      <tr>
          <th>Model</th>
          <th>Context Window</th>
          <th>Input Price/M</th>
          <th>Open Weight</th>
          <th>Notes</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Llama 4 Scout</strong></td>
          <td>10M tokens</td>
          <td>$0.08–$0.15</td>
          <td>Yes</td>
          <td>Provider-dependent cap</td>
      </tr>
      <tr>
          <td>Gemini 3 Pro</td>
          <td>10M tokens</td>
          <td>$12.00</td>
          <td>No</td>
          <td>Google Cloud only</td>
      </tr>
      <tr>
          <td>GPT-4.1</td>
          <td>1M tokens</td>
          <td>$2.00</td>
          <td>No</td>
          <td>OpenAI API</td>
      </tr>
      <tr>
          <td>Claude Opus 4.5</td>
          <td>200K tokens</td>
          <td>$25.00</td>
          <td>No</td>
          <td>Anthropic API</td>
      </tr>
      <tr>
          <td>Grok 4</td>
          <td>2M tokens</td>
          <td>$3.00</td>
          <td>No</td>
          <td>xAI API</td>
      </tr>
      <tr>
          <td>Llama 4 Maverick</td>
          <td>1M tokens</td>
          <td>$0.20</td>
          <td>Yes</td>
          <td>Scout&rsquo;s sibling model</td>
      </tr>
  </tbody>
</table>
<p>The cost angle is stark. For a 1M token codebase analysis job, Scout costs $0.08–$0.15. Claude Opus 4.5 costs $25 for the same prompt (even ignoring that it doesn&rsquo;t support 1M context). That&rsquo;s a 312× price difference for tasks where you specifically need large context. The caveat: providers cap Scout&rsquo;s actual context window. At launch, Groq supports up to 2M tokens, Together AI supports up to 4M, and Fireworks handles up to the full 10M on dedicated instances. The 10M theoretical maximum is only available via direct deployment.</p>
<h3 id="provider-availability-and-actual-context-limits">Provider Availability and Actual Context Limits</h3>
<p>The gap between &ldquo;model supports 10M tokens&rdquo; and &ldquo;you can actually use 10M tokens&rdquo; is significant. Shared inference infrastructure has memory constraints — most providers implement hard caps. Check each provider&rsquo;s current limits before designing workflows that depend on maximum context:</p>
<ul>
<li><strong>Groq</strong>: Fast inference ($0.11/M input, $0.34/M output), capped at 2M tokens</li>
<li><strong>Together AI</strong>: Up to 4M context, competitive pricing</li>
<li><strong>Fireworks</strong>: Full 10M on dedicated instances, higher cost</li>
<li><strong>OpenRouter</strong>: Aggregates providers, Scout available via multiple backends</li>
</ul>
<h2 id="api-quickstart-groq-together-ai-fireworks-and-openrouter">API Quickstart: Groq, Together AI, Fireworks, and OpenRouter</h2>
<p>Scout exposes an OpenAI-compatible API across all major providers, meaning zero code changes if you&rsquo;re switching from GPT-4. The model ID varies by provider, but the interface is identical. Groq is the fastest option for inference speed and the cheapest for output tokens at $0.34/M, though its context cap is 2M tokens. Together AI offers up to 4M context with slightly higher pricing. Fireworks is the only provider currently supporting Scout&rsquo;s full 10M context window via dedicated instances, but at a premium price. OpenRouter aggregates all three, letting you switch backends without changing your code. For most developers starting out, Groq is the right first choice — fast, cheap, and the API is live immediately after signup. Switch to Together AI or Fireworks once you need context windows larger than 2M tokens. All providers support streaming responses via <code>stream=True</code>, which is important for large-context requests where first-token latency is 15–60 seconds — streaming lets you show progress rather than blocking on a full response. Authentication follows the standard Bearer token pattern, and rate limits at the free tier are sufficient for development and testing.</p>
<h3 id="python-with-groq">Python with Groq</h3>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> groq <span style="color:#f92672">import</span> Groq
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> Groq(api_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your-groq-api-key&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">with</span> open(<span style="color:#e6db74">&#34;my_repo.txt&#34;</span>, <span style="color:#e6db74">&#34;r&#34;</span>) <span style="color:#66d9ef">as</span> f:
</span></span><span style="display:flex;"><span>    codebase <span style="color:#f92672">=</span> f<span style="color:#f92672">.</span>read()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;meta-llama/llama-4-scout-17b-16e-instruct&#34;</span>,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[
</span></span><span style="display:flex;"><span>        {
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>,
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;Here is the full codebase:</span><span style="color:#ae81ff">\n\n</span><span style="color:#e6db74">{</span>codebase<span style="color:#e6db74">}</span><span style="color:#ae81ff">\n\n</span><span style="color:#e6db74">Find all SQL injection vulnerabilities and explain the data flow for each one.&#34;</span>
</span></span><span style="display:flex;"><span>        }
</span></span><span style="display:flex;"><span>    ],
</span></span><span style="display:flex;"><span>    max_tokens<span style="color:#f92672">=</span><span style="color:#ae81ff">4096</span>,
</span></span><span style="display:flex;"><span>    temperature<span style="color:#f92672">=</span><span style="color:#ae81ff">0.1</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>print(response<span style="color:#f92672">.</span>choices[<span style="color:#ae81ff">0</span>]<span style="color:#f92672">.</span>message<span style="color:#f92672">.</span>content)
</span></span></code></pre></div><h3 id="python-with-together-ai-higher-context-limits">Python with Together AI (higher context limits)</h3>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> openai <span style="color:#f92672">import</span> OpenAI
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> OpenAI(
</span></span><span style="display:flex;"><span>    api_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;your-together-api-key&#34;</span>,
</span></span><span style="display:flex;"><span>    base_url<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;https://api.together.xyz/v1&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;meta-llama/Llama-4-Scout-17B-16E-Instruct&#34;</span>,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[
</span></span><span style="display:flex;"><span>        {<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;system&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;You are a senior software engineer performing codebase analysis.&#34;</span>},
</span></span><span style="display:flex;"><span>        {<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: codebase_content}
</span></span><span style="display:flex;"><span>    ],
</span></span><span style="display:flex;"><span>    max_tokens<span style="color:#f92672">=</span><span style="color:#ae81ff">8192</span>
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><h3 id="shellcurl-for-quick-tests">Shell/curl for quick tests</h3>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>curl https://api.groq.com/openai/v1/chat/completions <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  -H <span style="color:#e6db74">&#34;Authorization: Bearer </span>$GROQ_API_KEY<span style="color:#e6db74">&#34;</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  -H <span style="color:#e6db74">&#34;Content-Type: application/json&#34;</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  -d <span style="color:#e6db74">&#39;{
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    &#34;model&#34;: &#34;meta-llama/llama-4-scout-17b-16e-instruct&#34;,
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    &#34;messages&#34;: [{&#34;role&#34;: &#34;user&#34;, &#34;content&#34;: &#34;Analyze this code: ...&#34;}],
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    &#34;max_tokens&#34;: 2048
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">  }&#39;</span>
</span></span></code></pre></div><h2 id="full-codebase-analysis-without-rag-code-examples">Full Codebase Analysis Without RAG: Code Examples</h2>
<p>The most useful capability Scout unlocks is loading a complete repository and asking questions that require understanding the full codebase simultaneously. Here&rsquo;s a production-ready script for codebase loading:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> os
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> pathlib <span style="color:#f92672">import</span> Path
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> groq <span style="color:#f92672">import</span> Groq
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>SKIP_DIRS <span style="color:#f92672">=</span> {<span style="color:#e6db74">&#34;.git&#34;</span>, <span style="color:#e6db74">&#34;node_modules&#34;</span>, <span style="color:#e6db74">&#34;__pycache__&#34;</span>, <span style="color:#e6db74">&#34;.venv&#34;</span>, <span style="color:#e6db74">&#34;dist&#34;</span>, <span style="color:#e6db74">&#34;build&#34;</span>}
</span></span><span style="display:flex;"><span>SKIP_EXTENSIONS <span style="color:#f92672">=</span> {<span style="color:#e6db74">&#34;.png&#34;</span>, <span style="color:#e6db74">&#34;.jpg&#34;</span>, <span style="color:#e6db74">&#34;.gif&#34;</span>, <span style="color:#e6db74">&#34;.ico&#34;</span>, <span style="color:#e6db74">&#34;.lock&#34;</span>, <span style="color:#e6db74">&#34;.bin&#34;</span>, <span style="color:#e6db74">&#34;.wasm&#34;</span>}
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">load_codebase</span>(repo_path: str, max_tokens: int <span style="color:#f92672">=</span> <span style="color:#ae81ff">1_500_000</span>) <span style="color:#f92672">-&gt;</span> str:
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;&#34;&#34;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    Loads a repository into a single string for Scout.
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    Estimates tokens as len(text) / 4 (conservative).
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    &#34;&#34;&#34;</span>
</span></span><span style="display:flex;"><span>    files <span style="color:#f92672">=</span> []
</span></span><span style="display:flex;"><span>    total_chars <span style="color:#f92672">=</span> <span style="color:#ae81ff">0</span>
</span></span><span style="display:flex;"><span>    char_limit <span style="color:#f92672">=</span> max_tokens <span style="color:#f92672">*</span> <span style="color:#ae81ff">4</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">for</span> path <span style="color:#f92672">in</span> sorted(Path(repo_path)<span style="color:#f92672">.</span>rglob(<span style="color:#e6db74">&#34;*&#34;</span>)):
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">if</span> path<span style="color:#f92672">.</span>is_file():
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">if</span> any(d <span style="color:#f92672">in</span> path<span style="color:#f92672">.</span>parts <span style="color:#66d9ef">for</span> d <span style="color:#f92672">in</span> SKIP_DIRS):
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">continue</span>
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">if</span> path<span style="color:#f92672">.</span>suffix<span style="color:#f92672">.</span>lower() <span style="color:#f92672">in</span> SKIP_EXTENSIONS:
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">continue</span>
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">try</span>:
</span></span><span style="display:flex;"><span>                content <span style="color:#f92672">=</span> path<span style="color:#f92672">.</span>read_text(encoding<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;utf-8&#34;</span>, errors<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;ignore&#34;</span>)
</span></span><span style="display:flex;"><span>                relative <span style="color:#f92672">=</span> path<span style="color:#f92672">.</span>relative_to(repo_path)
</span></span><span style="display:flex;"><span>                entry <span style="color:#f92672">=</span> <span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;</span><span style="color:#ae81ff">\n\n</span><span style="color:#e6db74">### FILE: </span><span style="color:#e6db74">{</span>relative<span style="color:#e6db74">}</span><span style="color:#ae81ff">\n\n</span><span style="color:#e6db74">{</span>content<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">if</span> total_chars <span style="color:#f92672">+</span> len(entry) <span style="color:#f92672">&gt;</span> char_limit:
</span></span><span style="display:flex;"><span>                    <span style="color:#66d9ef">break</span>
</span></span><span style="display:flex;"><span>                files<span style="color:#f92672">.</span>append(entry)
</span></span><span style="display:flex;"><span>                total_chars <span style="color:#f92672">+=</span> len(entry)
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">except</span> <span style="color:#a6e22e">Exception</span>:
</span></span><span style="display:flex;"><span>                <span style="color:#66d9ef">continue</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> <span style="color:#e6db74">&#34;&#34;</span><span style="color:#f92672">.</span>join(files)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> Groq(api_key<span style="color:#f92672">=</span>os<span style="color:#f92672">.</span>environ[<span style="color:#e6db74">&#34;GROQ_API_KEY&#34;</span>])
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>codebase <span style="color:#f92672">=</span> load_codebase(<span style="color:#e6db74">&#34;/path/to/your/repo&#34;</span>)
</span></span><span style="display:flex;"><span>estimated_tokens <span style="color:#f92672">=</span> len(codebase) <span style="color:#f92672">//</span> <span style="color:#ae81ff">4</span>
</span></span><span style="display:flex;"><span>print(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;Loaded ~</span><span style="color:#e6db74">{</span>estimated_tokens<span style="color:#e6db74">:</span><span style="color:#e6db74">,</span><span style="color:#e6db74">}</span><span style="color:#e6db74"> tokens&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;meta-llama/llama-4-scout-17b-16e-instruct&#34;</span>,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[
</span></span><span style="display:flex;"><span>        {
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;system&#34;</span>,
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;You are a senior engineer reviewing a production codebase. Be specific and cite file paths.&#34;</span>
</span></span><span style="display:flex;"><span>        },
</span></span><span style="display:flex;"><span>        {
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>,
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;</span><span style="color:#e6db74">{</span>codebase<span style="color:#e6db74">}</span><span style="color:#ae81ff">\n\n</span><span style="color:#e6db74">---</span><span style="color:#ae81ff">\n\n</span><span style="color:#e6db74">Task: Generate a complete architectural diagram description covering all service boundaries, data flows, and external dependencies. Then list the top 5 technical debt items with their file locations.&#34;</span>
</span></span><span style="display:flex;"><span>        }
</span></span><span style="display:flex;"><span>    ],
</span></span><span style="display:flex;"><span>    max_tokens<span style="color:#f92672">=</span><span style="color:#ae81ff">4096</span>,
</span></span><span style="display:flex;"><span>    temperature<span style="color:#f92672">=</span><span style="color:#ae81ff">0.1</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>print(response<span style="color:#f92672">.</span>choices[<span style="color:#ae81ff">0</span>]<span style="color:#f92672">.</span>message<span style="color:#f92672">.</span>content)
</span></span></code></pre></div><p>This approach replaces a full RAG pipeline for repos under about 350,000 lines of code (at Groq&rsquo;s 2M context cap). For full 10M context, use Fireworks or deploy locally.</p>
<h2 id="multimodal-capabilities-combining-vision-and-long-context-text">Multimodal Capabilities: Combining Vision and Long-Context Text</h2>
<p>Llama 4 Scout is natively multimodal — it processes images and text in the same context window. This is genuinely useful for codebase analysis when combined with architecture diagrams. In practice, I&rsquo;ve used it to feed both a database ERD image and the full ORM layer code simultaneously, asking Scout to verify that the code correctly implements the relationships shown in the diagram. It catches discrepancies that pure code review misses because the diagram is the ground truth.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> base64
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">encode_image</span>(image_path: str) <span style="color:#f92672">-&gt;</span> str:
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">with</span> open(image_path, <span style="color:#e6db74">&#34;rb&#34;</span>) <span style="color:#66d9ef">as</span> f:
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> base64<span style="color:#f92672">.</span>b64encode(f<span style="color:#f92672">.</span>read())<span style="color:#f92672">.</span>decode(<span style="color:#e6db74">&#34;utf-8&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>arch_diagram <span style="color:#f92672">=</span> encode_image(<span style="color:#e6db74">&#34;architecture.png&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;meta-llama/llama-4-scout-17b-16e-instruct&#34;</span>,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[
</span></span><span style="display:flex;"><span>        {
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>,
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;content&#34;</span>: [
</span></span><span style="display:flex;"><span>                {
</span></span><span style="display:flex;"><span>                    <span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;image_url&#34;</span>,
</span></span><span style="display:flex;"><span>                    <span style="color:#e6db74">&#34;image_url&#34;</span>: {
</span></span><span style="display:flex;"><span>                        <span style="color:#e6db74">&#34;url&#34;</span>: <span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;data:image/png;base64,</span><span style="color:#e6db74">{</span>arch_diagram<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>
</span></span><span style="display:flex;"><span>                    }
</span></span><span style="display:flex;"><span>                },
</span></span><span style="display:flex;"><span>                {
</span></span><span style="display:flex;"><span>                    <span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;text&#34;</span>,
</span></span><span style="display:flex;"><span>                    <span style="color:#e6db74">&#34;text&#34;</span>: <span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;Here is the system architecture diagram. Now here is the full implementation:</span><span style="color:#ae81ff">\n\n</span><span style="color:#e6db74">{</span>codebase<span style="color:#e6db74">}</span><span style="color:#ae81ff">\n\n</span><span style="color:#e6db74">Identify where the implementation diverges from the architecture.&#34;</span>
</span></span><span style="display:flex;"><span>                }
</span></span><span style="display:flex;"><span>            ]
</span></span><span style="display:flex;"><span>        }
</span></span><span style="display:flex;"><span>    ],
</span></span><span style="display:flex;"><span>    max_tokens<span style="color:#f92672">=</span><span style="color:#ae81ff">4096</span>
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><p>This multimodal + long-context combination is one of Scout&rsquo;s strongest differentiators versus pure text models. No other open-weight model currently supports both 10M context and vision in the same request.</p>
<h2 id="local-deployment-with-ollama-and-vllm">Local Deployment with Ollama and vLLM</h2>
<p>Self-hosting Scout makes sense for teams with privacy requirements, regulated industries where code can&rsquo;t leave the premises, or workloads with high volume where API costs add up. The tradeoff: hardware requirements are substantial. The Q4_K_M quantized model weighs approximately 60GB, requiring either a single NVIDIA H100 80GB GPU or two RTX 4090s with VRAM pooling. Full FP16 inference requires approximately 218GB, meaning a minimum of three H100 80GB GPUs. For most teams, this puts local deployment in the &ldquo;dedicated inference server&rdquo; category rather than developer laptop territory. Ollama is the easiest deployment path — one command to pull and one command to run. vLLM offers higher throughput, better multi-user support via continuous batching and PagedAttention, and OpenAI-compatible API endpoints out of the box. The key practical limitation for local deployment: KV cache memory. At 10M tokens, the KV cache alone requires hundreds of GB beyond the model weights, which is why even local deployments practically cap context at 128K–512K tokens unless you have a multi-GPU setup specifically configured for long context. For teams processing entire repositories overnight (batch jobs, CI pipelines), local vLLM deployment with 512K context covers most real-world repos and completely eliminates per-token API costs after the hardware investment.</p>
<h3 id="ollama-setup-easiest-path">Ollama Setup (Easiest Path)</h3>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># Pull the Q4_K_M quantized model (~60GB download)</span>
</span></span><span style="display:flex;"><span>ollama pull llama4:scout
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Run inference</span>
</span></span><span style="display:flex;"><span>ollama run llama4:scout <span style="color:#e6db74">&#34;Analyze this code: ...&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Or via API</span>
</span></span><span style="display:flex;"><span>curl http://localhost:11434/api/chat <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  -d <span style="color:#e6db74">&#39;{
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    &#34;model&#34;: &#34;llama4:scout&#34;,
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    &#34;messages&#34;: [{&#34;role&#34;: &#34;user&#34;, &#34;content&#34;: &#34;Your prompt here&#34;}]
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">  }&#39;</span>
</span></span></code></pre></div><p>Hardware requirements for Ollama:</p>
<ul>
<li>Q4_K_M quantization: ~60GB VRAM or RAM</li>
<li>Recommended: 2× NVIDIA RTX 4090 (48GB total) or 1× H100 80GB</li>
<li>CPU-only inference is possible but very slow for large contexts (10+ minutes per request)</li>
</ul>
<p><strong>Important</strong>: Ollama&rsquo;s default context window is 2048 tokens. For large-context use you must explicitly set it:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>ollama run llama4:scout --ctx-size <span style="color:#ae81ff">131072</span>  <span style="color:#75715e"># 128K context</span>
</span></span></code></pre></div><p>Ollama doesn&rsquo;t currently support Scout&rsquo;s full 10M context window locally — at 60GB for Q4_K_M weights, the KV cache for 10M tokens would require hundreds of additional GB of memory. Practical local context is 128K–512K tokens depending on hardware.</p>
<h3 id="vllm-setup-production-self-hosting">vLLM Setup (Production Self-Hosting)</h3>
<p>For production self-hosting with higher throughput and full context support:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>pip install vllm
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>python -m vllm.entrypoints.openai.api_server <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --model meta-llama/Llama-4-Scout-17B-16E-Instruct <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --quantization awq <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --max-model-len <span style="color:#ae81ff">131072</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --gpu-memory-utilization 0.9 <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span><span style="color:#ae81ff"></span>  --tensor-parallel-size <span style="color:#ae81ff">1</span>
</span></span></code></pre></div><p>For full 10M context via vLLM, you need multiple H100s. The KV cache at 10M tokens in FP8 requires approximately 160GB additional GPU memory on top of the 55GB model weights — meaning a minimum of 3× H100 80GB for full 10M context in FP8. In practice, most teams using vLLM for Scout set <code>--max-model-len</code> to 500K–2M tokens, which is manageable on 2× H100.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> openai <span style="color:#f92672">import</span> OpenAI
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># vLLM exposes OpenAI-compatible API</span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> OpenAI(
</span></span><span style="display:flex;"><span>    api_key<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;not-needed&#34;</span>,
</span></span><span style="display:flex;"><span>    base_url<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;http://localhost:8000/v1&#34;</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;meta-llama/Llama-4-Scout-17B-16E-Instruct&#34;</span>,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[{<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: your_prompt}],
</span></span><span style="display:flex;"><span>    max_tokens<span style="color:#f92672">=</span><span style="color:#ae81ff">4096</span>
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><h2 id="production-considerations-latency-memory-and-provider-limits">Production Considerations: Latency, Memory, and Provider Limits</h2>
<p>Before deploying Scout in production, understand the latency reality. At full 10M tokens, first-token latency (TTFT) exceeds 60 seconds even on top-tier hardware. This is physics, not a bug — processing 10M tokens through a 17B active parameter transformer takes time regardless of hardware. Here&rsquo;s the practical latency breakdown by context size:</p>
<table>
  <thead>
      <tr>
          <th>Context Size</th>
          <th>Approx. TTFT (H100)</th>
          <th>Suitable For</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>10K tokens</td>
          <td>&lt; 1 second</td>
          <td>Interactive chat</td>
      </tr>
      <tr>
          <td>128K tokens</td>
          <td>2–5 seconds</td>
          <td>Near-real-time analysis</td>
      </tr>
      <tr>
          <td>1M tokens</td>
          <td>15–30 seconds</td>
          <td>Async workflows</td>
      </tr>
      <tr>
          <td>10M tokens</td>
          <td>60+ seconds</td>
          <td>Batch jobs only</td>
      </tr>
  </tbody>
</table>
<p>For production use cases, architect around Scout&rsquo;s latency profile:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> asyncio
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> groq <span style="color:#f92672">import</span> AsyncGroq
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">async</span> <span style="color:#66d9ef">def</span> <span style="color:#a6e22e">analyze_batch</span>(codebases: list[str]) <span style="color:#f92672">-&gt;</span> list[str]:
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;&#34;&#34;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    Run multiple codebase analyses concurrently.
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    Scout&#39;s latency at large contexts means async/parallel is essential.
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">    &#34;&#34;&#34;</span>
</span></span><span style="display:flex;"><span>    client <span style="color:#f92672">=</span> AsyncGroq(api_key<span style="color:#f92672">=</span>os<span style="color:#f92672">.</span>environ[<span style="color:#e6db74">&#34;GROQ_API_KEY&#34;</span>])
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    tasks <span style="color:#f92672">=</span> [
</span></span><span style="display:flex;"><span>        client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>            model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;meta-llama/llama-4-scout-17b-16e-instruct&#34;</span>,
</span></span><span style="display:flex;"><span>            messages<span style="color:#f92672">=</span>[{<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: code}],
</span></span><span style="display:flex;"><span>            max_tokens<span style="color:#f92672">=</span><span style="color:#ae81ff">2048</span>
</span></span><span style="display:flex;"><span>        )
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">for</span> code <span style="color:#f92672">in</span> codebases
</span></span><span style="display:flex;"><span>    ]
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>    results <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> asyncio<span style="color:#f92672">.</span>gather(<span style="color:#f92672">*</span>tasks)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> [r<span style="color:#f92672">.</span>choices[<span style="color:#ae81ff">0</span>]<span style="color:#f92672">.</span>message<span style="color:#f92672">.</span>content <span style="color:#66d9ef">for</span> r <span style="color:#f92672">in</span> results]
</span></span></code></pre></div><p>Scout fits well in nightly batch analysis jobs, CI/CD pipeline checks that run after merge (not blocking merge), and async security scanning. It doesn&rsquo;t fit interactive developer tools where latency matters.</p>
<h2 id="real-world-use-cases-beyond-codebase-analysis">Real-World Use Cases Beyond Codebase Analysis</h2>
<p>Scout&rsquo;s 10M context window opens workflows beyond just &ldquo;load the whole repo&rdquo;:</p>
<p><strong>Security audits</strong>: Load the full codebase, dependency list, and CVE database for the known libraries — all in one pass. Ask Scout to identify code paths that use vulnerable library functions. This cross-references three information sources simultaneously without any retrieval logic.</p>
<p><strong>Legacy code migration</strong>: Load the full legacy Python 2 codebase plus the target Python 3 style guide plus 500 existing migration examples. Ask Scout to generate migration patches that match your team&rsquo;s established patterns — it has enough context to maintain consistency across the entire migration.</p>
<p><strong>API contract verification</strong>: Load all client SDKs, backend handler implementations, and OpenAPI spec simultaneously. Ask Scout to identify every place where client expectations diverge from server implementations. This kind of cross-file, cross-service analysis is nearly impossible with chunked retrieval.</p>
<p><strong>Onboarding documentation generation</strong>: Load the codebase, existing ADRs, and git history summaries. Ask Scout to generate comprehensive onboarding docs that explain not just what the code does but why key architectural decisions were made.</p>
<p><strong>Compliance checking</strong>: Load the full codebase plus GDPR/HIPAA/SOC2 requirement checklists. Ask Scout to produce a compliance report citing specific file locations where requirements are or aren&rsquo;t met.</p>
<h2 id="when-not-to-use-scout--matching-context-to-your-workload">When NOT to Use Scout — Matching Context to Your Workload</h2>
<p>Scout&rsquo;s 10M context window is genuinely useful for the cases above, but it&rsquo;s the wrong choice for many common tasks. Using Scout for small-context tasks wastes money and adds latency.</p>
<p><strong>Don&rsquo;t use Scout for</strong>:</p>
<ul>
<li>Single-function code review (use a smaller model with 8K context — 1000× cheaper)</li>
<li>Simple code generation (Claude Sonnet or GPT-4o-mini are faster and cheaper)</li>
<li>Real-time autocomplete (latency makes Scout unsuitable for IDE integration)</li>
<li>Chat interactions where responses need to be under 3 seconds</li>
<li>Any task where the total prompt is under 100K tokens and you don&rsquo;t need the model&rsquo;s coding benchmark performance</li>
</ul>
<p><strong>Context-right-sizing guide</strong>:</p>
<table>
  <thead>
      <tr>
          <th>Task</th>
          <th>Context Needed</th>
          <th>Recommended Model</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Single function review</td>
          <td>&lt; 8K</td>
          <td>Claude Haiku, GPT-4o-mini</td>
      </tr>
      <tr>
          <td>Single file review</td>
          <td>&lt; 32K</td>
          <td>GPT-4o, Gemini Flash</td>
      </tr>
      <tr>
          <td>Module/package review</td>
          <td>32K–200K</td>
          <td>Claude Sonnet 4.6, GPT-4o</td>
      </tr>
      <tr>
          <td>Full service review</td>
          <td>200K–2M</td>
          <td>Llama 4 Scout (API)</td>
      </tr>
      <tr>
          <td>Full repository audit</td>
          <td>2M–10M</td>
          <td>Llama 4 Scout (dedicated)</td>
      </tr>
  </tbody>
</table>
<p>In practice: use Scout when you need to analyze something that genuinely requires seeing multiple files together, the combined context exceeds what other models support, and latency isn&rsquo;t a hard constraint. For everything else, a smaller model is faster, cheaper, and often just as accurate for the narrower task.</p>
<hr>
<h2 id="faq">FAQ</h2>
<p><strong>What is Llama 4 Scout&rsquo;s actual context window limit?</strong></p>
<p>The model supports 10 million tokens natively, but available context depends on the provider. Groq caps at 2M tokens, Together AI supports up to 4M, and Fireworks offers full 10M on dedicated instances. Local deployment via vLLM can reach 10M with sufficient hardware (3+ H100 80GB GPUs for full context in FP8).</p>
<p><strong>How much does Llama 4 Scout cost per API call?</strong></p>
<p>Groq charges $0.11 per million input tokens and $0.34 per million output tokens. Other providers range from $0.08 to $0.15/M input. Loading a 1M token codebase for analysis costs roughly $0.11–$0.15, far less than comparable large-context proprietary models like Gemini 3 Pro ($12/M input) or Claude Opus 4.5 ($25/M input).</p>
<p><strong>Can Llama 4 Scout run on a consumer GPU?</strong></p>
<p>The Q4_K_M quantized model requires approximately 60GB of VRAM or RAM. This fits on 2× RTX 4090 (48GB, with some context limitations) or a single H100 80GB. Consumer GPUs like the RTX 4090 (24GB) can&rsquo;t run it in VRAM alone — you&rsquo;d need CPU offloading via Ollama, which degrades performance significantly. For most developers, the cloud API is more practical than local deployment.</p>
<p><strong>Is Llama 4 Scout better than GPT-4o for code analysis?</strong></p>
<p>On the MBPP coding benchmark, Scout scores 68%, beating GPT-4o. More importantly, for full-repository analysis tasks, Scout&rsquo;s 10M context window means it literally has access to more code at once, which tends to produce better cross-file analysis regardless of raw benchmark scores. For single-function tasks, GPT-4o is faster and comparably accurate. The right choice depends on the scope of the analysis.</p>
<p><strong>How do I load a large codebase into Scout efficiently?</strong></p>
<p>Flatten the repository into a single text string with file path headers, filter out binary files and generated directories (node_modules, .git, dist), and estimate token count at roughly 4 tokens per line of code. A 500K-line codebase becomes approximately 2M tokens — well within Groq&rsquo;s 2M limit and comfortably within Together AI&rsquo;s 4M limit. See the code examples above for a production-ready loader implementation.</p>
]]></content:encoded></item><item><title>GPT-5.4 API Developer Guide 2026: 1M Context, Computer Use, and 5 Reasoning Levels</title><link>https://baeseokjae.github.io/posts/gpt-5-4-api-developer-guide-2026/</link><pubDate>Thu, 30 Apr 2026 09:04:26 +0000</pubDate><guid>https://baeseokjae.github.io/posts/gpt-5-4-api-developer-guide-2026/</guid><description>Complete GPT-5.4 API guide: 1M token context, 5 reasoning effort levels, native computer use, pricing tiers, and migration from gpt-4o/gpt-5.2.</description><content:encoded><![CDATA[<p>GPT-5.4 is OpenAI&rsquo;s most capable general-purpose model as of 2026, combining a 1,050,000-token context window, native computer use at 75% OSWorld accuracy, and five tunable reasoning effort levels in a single Chat Completions API drop-in. Released March 5, 2026, it replaces gpt-5.2 for most production workloads with no endpoint change required.</p>
<h2 id="what-is-gpt-54-release-date-model-variants-and-whats-new">What Is GPT-5.4? Release Date, Model Variants, and What&rsquo;s New</h2>
<p>GPT-5.4 is OpenAI&rsquo;s flagship general-purpose language model released on March 5, 2026, and it represents the first mainline model to combine frontier reasoning, native computer control, and a 1-million-token context window in a single architecture. Unlike earlier specialized variants — o3 for reasoning or gpt-5.2 for general use — GPT-5.4 integrates GPT-5.3-codex coding capabilities directly, making it a unified backbone for agentic, analytical, and conversational workloads. On launch day, it scored 75.0% on the OSWorld-Verified computer use benchmark, surpassing the human expert baseline of 72.4% — a first for any general-purpose model. On knowledge work (GDPval), GPT-5.4 matches or outperforms industry professionals in 83% of comparisons across 44 occupations. There are two production variants: <strong>gpt-5.4</strong> (the standard model, priced at $2.50/$15 per million input/output tokens) and <strong>gpt-5.4-pro</strong> (optimized for high-stakes enterprise tasks at $30/$180 per million input/output tokens). Both share the same API surface and context window; the pro variant allocates more compute budget per inference by default.</p>
<h3 id="model-variants-at-a-glance">Model Variants at a Glance</h3>
<table>
  <thead>
      <tr>
          <th>Model</th>
          <th>Input Price</th>
          <th>Output Price</th>
          <th>Best For</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>gpt-5.4</td>
          <td>$2.50/M tokens</td>
          <td>$15/M tokens</td>
          <td>General API workloads, agents</td>
      </tr>
      <tr>
          <td>gpt-5.4-pro</td>
          <td>$30/M tokens</td>
          <td>$180/M tokens</td>
          <td>High-stakes enterprise, legal, finance</td>
      </tr>
      <tr>
          <td>gpt-5.4-mini</td>
          <td>See pricing page</td>
          <td>See pricing page</td>
          <td>Cost-sensitive high-volume use</td>
      </tr>
  </tbody>
</table>
<p>The <strong>gpt-5.4-mini</strong> variant is aimed at cost-sensitive, high-volume tasks like classification, extraction, and routing — where full reasoning headroom is unnecessary. For the majority of production API use, gpt-5.4 with tuned <code>reasoning.effort</code> is the right starting point.</p>
<h2 id="gpt-54-api-access-endpoints-authentication-and-drop-in-migration">GPT-5.4 API Access: Endpoints, Authentication, and Drop-In Migration</h2>
<p>GPT-5.4 is a drop-in replacement for any Chat Completions API caller — you change the <code>model</code> string and gain new capabilities immediately. The endpoint, authentication headers, message format, and streaming protocol are identical to gpt-4o and gpt-5.2. Organizations already on gpt-4o can migrate in under five minutes by updating a single environment variable. For gpt-5.2 users, the migration is equally straightforward: all previous parameters (<code>temperature</code>, <code>top_p</code>, <code>tool_choice</code>, <code>response_format</code>) continue to work unchanged. New GPT-5.4-specific parameters (<code>reasoning.effort</code>, <code>computer_use</code>) are optional — omitting them returns behavior equivalent to a standard chat completion. This backward compatibility is intentional: OpenAI&rsquo;s published migration guide classifies gpt-5.4 as a drop-in upgrade, not a breaking API change. Authentication uses the same <code>Authorization: Bearer sk-...</code> header and OpenAI SDK versions ≥ 2.0.0 include native GPT-5.4 support.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">from</span> openai <span style="color:#f92672">import</span> OpenAI
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>client <span style="color:#f92672">=</span> OpenAI()  <span style="color:#75715e"># reads OPENAI_API_KEY from environment</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;gpt-5.4&#34;</span>,           <span style="color:#75715e"># only change needed from gpt-4o</span>
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[
</span></span><span style="display:flex;"><span>        {<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;Explain the 1M context window use cases.&#34;</span>}
</span></span><span style="display:flex;"><span>    ]
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>print(response<span style="color:#f92672">.</span>choices[<span style="color:#ae81ff">0</span>]<span style="color:#f92672">.</span>message<span style="color:#f92672">.</span>content)
</span></span></code></pre></div><p>For teams using the Node.js SDK:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-typescript" data-lang="typescript"><span style="display:flex;"><span><span style="color:#66d9ef">import</span> <span style="color:#a6e22e">OpenAI</span> <span style="color:#66d9ef">from</span> <span style="color:#e6db74">&#34;openai&#34;</span>;
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">client</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">new</span> <span style="color:#a6e22e">OpenAI</span>();
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">const</span> <span style="color:#a6e22e">response</span> <span style="color:#f92672">=</span> <span style="color:#66d9ef">await</span> <span style="color:#a6e22e">client</span>.<span style="color:#a6e22e">chat</span>.<span style="color:#a6e22e">completions</span>.<span style="color:#a6e22e">create</span>({
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">model</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;gpt-5.4&#34;</span>,
</span></span><span style="display:flex;"><span>  <span style="color:#a6e22e">messages</span><span style="color:#f92672">:</span> [{ <span style="color:#a6e22e">role</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#a6e22e">content</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;What changed in GPT-5.4?&#34;</span> }],
</span></span><span style="display:flex;"><span>});
</span></span></code></pre></div><p>Both examples require zero structural changes beyond the model name.</p>
<h2 id="the-5-reasoning-effort-levels-explained-none-to-xhigh-with-code-examples">The 5 Reasoning Effort Levels Explained (none to xhigh) with Code Examples</h2>
<p>The <code>reasoning.effort</code> parameter is GPT-5.4&rsquo;s most important new knob for production developers, offering five distinct levels — <code>none</code>, <code>low</code>, <code>medium</code>, <code>high</code>, and <code>xhigh</code> — that directly control the model&rsquo;s internal chain-of-thought budget, latency, and per-token cost. At <code>none</code>, the model skips explicit reasoning steps and behaves like a fast instruct model; at <code>xhigh</code>, it allocates maximum compute to multi-step reasoning, which can cost 3–5x more than the same request at <code>low</code>. This replaces the blunt <code>temperature</code> dial for controlling response quality: you now tune <em>reasoning depth</em> rather than sampling randomness. For most production routing tasks, <code>low</code> or <code>medium</code> is sufficient. For code synthesis, complex analysis, or agentic planning where accuracy is critical, <code>high</code> or <code>xhigh</code> pays for itself by reducing error-correction loops. OpenAI benchmarks show that xhigh produces measurably better SWE-bench results (57.7%) than medium or low settings on the same model weights.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#75715e"># Classification / routing — low effort, fast and cheap</span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;gpt-5.4&#34;</span>,
</span></span><span style="display:flex;"><span>    reasoning<span style="color:#f92672">=</span>{<span style="color:#e6db74">&#34;effort&#34;</span>: <span style="color:#e6db74">&#34;low&#34;</span>},
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[{<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;Classify this support ticket: &#39;My invoice is wrong&#39;&#34;</span>}]
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Complex code synthesis — xhigh effort</span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;gpt-5.4&#34;</span>,
</span></span><span style="display:flex;"><span>    reasoning<span style="color:#f92672">=</span>{<span style="color:#e6db74">&#34;effort&#34;</span>: <span style="color:#e6db74">&#34;xhigh&#34;</span>},
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[{<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;Refactor this 800-line Python module to async...&#34;</span>}]
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><h3 id="choosing-the-right-effort-level">Choosing the Right Effort Level</h3>
<table>
  <thead>
      <tr>
          <th>Task Type</th>
          <th>Recommended Effort</th>
          <th>Relative Cost</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Classification, routing, extraction</td>
          <td>none / low</td>
          <td>1x</td>
      </tr>
      <tr>
          <td>Summarization, Q&amp;A over documents</td>
          <td>medium</td>
          <td>1.5–2x</td>
      </tr>
      <tr>
          <td>Code review, debugging, analysis</td>
          <td>high</td>
          <td>2–3x</td>
      </tr>
      <tr>
          <td>Complex agentic planning, synthesis</td>
          <td>xhigh</td>
          <td>3–5x</td>
      </tr>
  </tbody>
</table>
<p>A practical production strategy: default to <code>medium</code>, then gate <code>xhigh</code> behind a fallback path triggered when <code>medium</code> returns low-confidence outputs. This pattern cuts mean inference cost by 40–60% compared to running all requests at <code>xhigh</code>.</p>
<h3 id="latency-expectations-by-effort-level">Latency Expectations by Effort Level</h3>
<p><code>none</code> and <code>low</code> typically return first tokens in under 1 second for short prompts. <code>xhigh</code> on a complex prompt may take 10–30 seconds before streaming begins. Design your UX accordingly — streaming with <code>stream: true</code> is strongly recommended for <code>high</code> and <code>xhigh</code> requests so users see incremental output.</p>
<h2 id="1-million-token-context-window-how-it-works-and-when-to-use-it">1 Million Token Context Window: How It Works and When to Use It</h2>
<p>GPT-5.4 supports up to 1,050,000 input tokens in a single request, making it the first general-purpose model capable of ingesting an entire large codebase, a full book, or months of agent conversation history in one API call. The context window is priced in two tiers: input tokens up to 272,000 are billed at the standard rate ($2.50/M for gpt-5.4), while tokens beyond that threshold cost 2x input and 1.5x output. This means loading a 500K-token codebase costs roughly $3.00 in input fees at standard pricing — a viable alternative to building complex chunking and retrieval pipelines for many teams. The practical effect is that tasks previously requiring RAG infrastructure (embedding databases, chunking logic, retrieval tuning) can now be handled with a single structured prompt. That said, long-context requests still increase latency proportionally, and the model&rsquo;s effective attention over very long inputs degrades for needles buried deep in a haystack — retrieval-augmented approaches remain superior for precise lookup across millions of documents.</p>
<h3 id="practical-1m-context-use-cases">Practical 1M Context Use Cases</h3>
<p><strong>Full codebase analysis:</strong> Load an entire monorepo (300K–600K tokens) and ask GPT-5.4 to trace a bug across files, identify dead code, or generate a migration plan. No chunking, no embedding index.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> os
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Load all Python files in a repo</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">load_repo</span>(path: str) <span style="color:#f92672">-&gt;</span> str:
</span></span><span style="display:flex;"><span>    files <span style="color:#f92672">=</span> []
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">for</span> root, _, fnames <span style="color:#f92672">in</span> os<span style="color:#f92672">.</span>walk(path):
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">for</span> fname <span style="color:#f92672">in</span> fnames:
</span></span><span style="display:flex;"><span>            <span style="color:#66d9ef">if</span> fname<span style="color:#f92672">.</span>endswith(<span style="color:#e6db74">&#34;.py&#34;</span>):
</span></span><span style="display:flex;"><span>                fpath <span style="color:#f92672">=</span> os<span style="color:#f92672">.</span>path<span style="color:#f92672">.</span>join(root, fname)
</span></span><span style="display:flex;"><span>                content <span style="color:#f92672">=</span> open(fpath)<span style="color:#f92672">.</span>read()
</span></span><span style="display:flex;"><span>                files<span style="color:#f92672">.</span>append(<span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;### </span><span style="color:#e6db74">{</span>fpath<span style="color:#e6db74">}</span><span style="color:#ae81ff">\n</span><span style="color:#e6db74">{</span>content<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> <span style="color:#e6db74">&#34;</span><span style="color:#ae81ff">\n\n</span><span style="color:#e6db74">&#34;</span><span style="color:#f92672">.</span>join(files)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>repo_content <span style="color:#f92672">=</span> load_repo(<span style="color:#e6db74">&#34;./my-project&#34;</span>)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;gpt-5.4&#34;</span>,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[
</span></span><span style="display:flex;"><span>        {<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;system&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;You are a senior code reviewer.&#34;</span>},
</span></span><span style="display:flex;"><span>        {<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;Review this codebase for security issues:</span><span style="color:#ae81ff">\n\n</span><span style="color:#e6db74">{</span>repo_content<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>}
</span></span><span style="display:flex;"><span>    ]
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><p><strong>Long agent session replay:</strong> Persist full conversation history across days or weeks of an agentic workflow and let GPT-5.4 reason over its own prior decisions. This eliminates the need for external memory stores in many agent architectures.</p>
<p><strong>Multi-document contract analysis:</strong> Load 50+ contract PDFs (converted to text) in one request and ask the model to identify conflicting clauses across documents — a task that was previously impractical without custom orchestration.</p>
<h3 id="when-not-to-use-1m-context">When Not to Use 1M Context</h3>
<ul>
<li>When you need sub-second latency — large contexts add seconds of prefill time</li>
<li>When the same document set is queried repeatedly — a vector index is more economical than re-sending 500K tokens each time</li>
<li>When the relevant information is localized — use targeted retrieval and a shorter context for 10x lower cost</li>
</ul>
<h2 id="native-computer-use-api-architecture-osworld-75-benchmark-and-getting-started">Native Computer Use API: Architecture, OSWorld 75% Benchmark, and Getting Started</h2>
<p>GPT-5.4&rsquo;s computer use capability is a first-class API feature that enables the model to observe a desktop environment through screenshots and issue mouse clicks, keyboard input, and application commands — without requiring custom automation code like Selenium or Playwright. At 75.0% on the OSWorld-Verified benchmark (surpassing the 72.4% human expert baseline), GPT-5.4 is the most capable general-purpose model for desktop automation as of 2026. The architecture is straightforward: you send the model a screenshot (base64 or URL), describe a task, and receive a structured action response that your client executes. The model then receives the updated screenshot and continues the loop until the task is complete. GPT-5.4 processes screenshots at full resolution with native vision — no downscaling or OCR preprocessing required — which significantly improves accuracy on small UI elements like checkboxes, dropdown menus, and text fields that trip up lower-resolution approaches.</p>
<h3 id="computer-use-api-quick-start">Computer Use API Quick Start</h3>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> base64
</span></span><span style="display:flex;"><span><span style="color:#f92672">from</span> PIL <span style="color:#f92672">import</span> ImageGrab
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">get_screenshot</span>() <span style="color:#f92672">-&gt;</span> str:
</span></span><span style="display:flex;"><span>    img <span style="color:#f92672">=</span> ImageGrab<span style="color:#f92672">.</span>grab()
</span></span><span style="display:flex;"><span>    img<span style="color:#f92672">.</span>save(<span style="color:#e6db74">&#34;/tmp/screen.png&#34;</span>)
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">with</span> open(<span style="color:#e6db74">&#34;/tmp/screen.png&#34;</span>, <span style="color:#e6db74">&#34;rb&#34;</span>) <span style="color:#66d9ef">as</span> f:
</span></span><span style="display:flex;"><span>        <span style="color:#66d9ef">return</span> base64<span style="color:#f92672">.</span>b64encode(f<span style="color:#f92672">.</span>read())<span style="color:#f92672">.</span>decode()
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">def</span> <span style="color:#a6e22e">computer_use_step</span>(screenshot_b64: str, task: str, history: list) <span style="color:#f92672">-&gt;</span> dict:
</span></span><span style="display:flex;"><span>    response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>        model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;gpt-5.4&#34;</span>,
</span></span><span style="display:flex;"><span>        tools<span style="color:#f92672">=</span>[{<span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;computer_use&#34;</span>}],
</span></span><span style="display:flex;"><span>        tool_choice<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;required&#34;</span>,
</span></span><span style="display:flex;"><span>        messages<span style="color:#f92672">=</span>history <span style="color:#f92672">+</span> [
</span></span><span style="display:flex;"><span>            {
</span></span><span style="display:flex;"><span>                <span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>,
</span></span><span style="display:flex;"><span>                <span style="color:#e6db74">&#34;content&#34;</span>: [
</span></span><span style="display:flex;"><span>                    {<span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;image_url&#34;</span>, <span style="color:#e6db74">&#34;image_url&#34;</span>: {<span style="color:#e6db74">&#34;url&#34;</span>: <span style="color:#e6db74">f</span><span style="color:#e6db74">&#34;data:image/png;base64,</span><span style="color:#e6db74">{</span>screenshot_b64<span style="color:#e6db74">}</span><span style="color:#e6db74">&#34;</span>}},
</span></span><span style="display:flex;"><span>                    {<span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;text&#34;</span>, <span style="color:#e6db74">&#34;text&#34;</span>: task}
</span></span><span style="display:flex;"><span>                ]
</span></span><span style="display:flex;"><span>            }
</span></span><span style="display:flex;"><span>        ]
</span></span><span style="display:flex;"><span>    )
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">return</span> response<span style="color:#f92672">.</span>choices[<span style="color:#ae81ff">0</span>]<span style="color:#f92672">.</span>message
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># Example: automate a browser form</span>
</span></span><span style="display:flex;"><span>history <span style="color:#f92672">=</span> [{<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;system&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;You control a desktop. Complete tasks by clicking and typing.&#34;</span>}]
</span></span><span style="display:flex;"><span>task <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;Open Chrome, navigate to example.com, and fill in the contact form with test data.&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>screenshot <span style="color:#f92672">=</span> get_screenshot()
</span></span><span style="display:flex;"><span>action <span style="color:#f92672">=</span> computer_use_step(screenshot, task, history)
</span></span><span style="display:flex;"><span>print(action)  <span style="color:#75715e"># {&#34;tool_calls&#34;: [{&#34;type&#34;: &#34;computer_use&#34;, &#34;action&#34;: &#34;click&#34;, &#34;x&#34;: 450, &#34;y&#34;: 230}]}</span>
</span></span></code></pre></div><h3 id="what-computer-use-replaces">What Computer Use Replaces</h3>
<p>GPT-5.4&rsquo;s computer use scores above human baseline on OSWorld tasks that previously required brittle selector-based automation (Selenium, Playwright) or specialized RPA tools (UiPath, Automation Anywhere). For exploratory or exception-heavy workflows — where the UI state varies and hard-coded selectors break — the vision-driven approach is substantially more robust. For high-volume, predictable workflows with stable UIs, traditional automation remains faster and cheaper.</p>
<h2 id="full-tool-support-overview-function-calling-tool-search-mcp-hosted-shell">Full Tool Support Overview: Function Calling, Tool Search, MCP, Hosted Shell</h2>
<p>GPT-5.4 supports the complete OpenAI tool ecosystem: function calling (same syntax as gpt-4o), tool search for dynamic capability discovery, Model Context Protocol (MCP) for structured external integrations, hosted shell for sandboxed code execution, and computer use for desktop automation. All tools can be combined in a single request — the model reasons over which tools to invoke and in what order, making it a capable orchestrator for multi-step agentic pipelines. Tool search is new in gpt-5.4: rather than defining all available tools upfront, you can expose a tool catalog and let the model retrieve and call relevant tools dynamically, significantly reducing prompt size for large tool ecosystems.</p>
<p>Function calling syntax is unchanged from previous models:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span>tools <span style="color:#f92672">=</span> [
</span></span><span style="display:flex;"><span>    {
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;function&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#e6db74">&#34;function&#34;</span>: {
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;name&#34;</span>: <span style="color:#e6db74">&#34;get_weather&#34;</span>,
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;description&#34;</span>: <span style="color:#e6db74">&#34;Get current weather for a location&#34;</span>,
</span></span><span style="display:flex;"><span>            <span style="color:#e6db74">&#34;parameters&#34;</span>: {
</span></span><span style="display:flex;"><span>                <span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;object&#34;</span>,
</span></span><span style="display:flex;"><span>                <span style="color:#e6db74">&#34;properties&#34;</span>: {
</span></span><span style="display:flex;"><span>                    <span style="color:#e6db74">&#34;location&#34;</span>: {<span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;string&#34;</span>, <span style="color:#e6db74">&#34;description&#34;</span>: <span style="color:#e6db74">&#34;City name&#34;</span>},
</span></span><span style="display:flex;"><span>                    <span style="color:#e6db74">&#34;unit&#34;</span>: {<span style="color:#e6db74">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;string&#34;</span>, <span style="color:#e6db74">&#34;enum&#34;</span>: [<span style="color:#e6db74">&#34;celsius&#34;</span>, <span style="color:#e6db74">&#34;fahrenheit&#34;</span>]}
</span></span><span style="display:flex;"><span>                },
</span></span><span style="display:flex;"><span>                <span style="color:#e6db74">&#34;required&#34;</span>: [<span style="color:#e6db74">&#34;location&#34;</span>]
</span></span><span style="display:flex;"><span>            }
</span></span><span style="display:flex;"><span>        }
</span></span><span style="display:flex;"><span>    }
</span></span><span style="display:flex;"><span>]
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>response <span style="color:#f92672">=</span> client<span style="color:#f92672">.</span>chat<span style="color:#f92672">.</span>completions<span style="color:#f92672">.</span>create(
</span></span><span style="display:flex;"><span>    model<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;gpt-5.4&#34;</span>,
</span></span><span style="display:flex;"><span>    tools<span style="color:#f92672">=</span>tools,
</span></span><span style="display:flex;"><span>    messages<span style="color:#f92672">=</span>[{<span style="color:#e6db74">&#34;role&#34;</span>: <span style="color:#e6db74">&#34;user&#34;</span>, <span style="color:#e6db74">&#34;content&#34;</span>: <span style="color:#e6db74">&#34;What&#39;s the weather in Tokyo?&#34;</span>}]
</span></span><span style="display:flex;"><span>)
</span></span></code></pre></div><p>MCP integration allows GPT-5.4 to connect to external systems (databases, APIs, file systems) through a standardized protocol, enabling persistent agent sessions with consistent tool interfaces across turns.</p>
<h2 id="gpt-54-api-pricing-deep-dive-standard-vs-pro-vs-long-context-tiers">GPT-5.4 API Pricing Deep Dive: Standard vs. Pro vs. Long-Context Tiers</h2>
<p>GPT-5.4 pricing has three dimensions that developers must account for: model tier (standard vs. pro), context length (standard vs. long-context surcharge), and output volume. Standard gpt-5.4 costs $2.50 per million input tokens and $15 per million output tokens — roughly 2x the cost of gpt-4o at launch but with substantially higher capability. GPT-5.4-pro costs $30/$180 per million input/output tokens, a 12x premium over standard, justified for regulated industries (legal, finance, healthcare) where higher accuracy reduces downstream error costs. The long-context surcharge applies beyond 272,000 input tokens: input pricing doubles to $5/M and output pricing rises to $22.50/M for gpt-5.4 standard. For gpt-5.4-pro, long-context rates scale proportionally. This pricing structure rewards efficient context management — trimming system prompts, compressing conversation history, and using retrieval to avoid unnecessary long-context billing is directly profitable at scale.</p>
<h3 id="cost-estimation-examples">Cost Estimation Examples</h3>
<table>
  <thead>
      <tr>
          <th>Workload</th>
          <th>Model</th>
          <th>Tokens</th>
          <th>Estimated Cost</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Simple Q&amp;A</td>
          <td>gpt-5.4</td>
          <td>1K in / 500 out</td>
          <td>~$0.010</td>
      </tr>
      <tr>
          <td>Document summary (50 pages)</td>
          <td>gpt-5.4</td>
          <td>40K in / 2K out</td>
          <td>~$0.130</td>
      </tr>
      <tr>
          <td>Full codebase review (400K tokens)</td>
          <td>gpt-5.4</td>
          <td>400K in / 5K out</td>
          <td>~$2.075</td>
      </tr>
      <tr>
          <td>Enterprise contract analysis</td>
          <td>gpt-5.4-pro</td>
          <td>200K in / 10K out</td>
          <td>~$7.80</td>
      </tr>
  </tbody>
</table>
<p>For high-volume workloads (&gt;10M tokens/day), OpenAI offers volume pricing tiers — contact OpenAI sales for enterprise pricing.</p>
<h3 id="when-to-choose-gpt-54-vs-gpt-54-pro">When to Choose gpt-5.4 vs. gpt-5.4-pro</h3>
<p>Choose <strong>gpt-5.4</strong> for: API integrations, code generation, agentic workflows, content generation, and any workload where errors are recoverable.</p>
<p>Choose <strong>gpt-5.4-pro</strong> for: legal document review, financial modeling, medical information synthesis, or any domain where a single error has significant downstream cost exceeding the $30/M input premium.</p>
<h2 id="gpt-54-vs-claude-opus-46-vs-gemini-31-pro-benchmark-comparison">GPT-5.4 vs. Claude Opus 4.6 vs. Gemini 3.1 Pro: Benchmark Comparison</h2>
<p>GPT-5.4 leads on computer use and knowledge work benchmarks but faces strong competition from Claude Opus 4.6 (Anthropic) and Gemini 3.1 Pro (Google) on coding and long-document reasoning tasks. On the OSWorld-Verified benchmark, GPT-5.4 scores 75.0% — Claude Opus 4.6 scores approximately 68% and Gemini 3.1 Pro reaches roughly 62%, making GPT-5.4 the clear leader for desktop automation. On SWE-bench Pro (real-world software engineering), GPT-5.4 at xhigh reasoning scores 57.7%, which is competitive with but not dominant over Claude Opus 4.6&rsquo;s reported 55–58% range. On GDPval (knowledge work across 44 occupations), GPT-5.4 leads at 83% versus the mid-70s for competitors. For developers choosing a flagship model, the decision typically hinges on use case: GPT-5.4 for computer use and knowledge work, Claude Opus 4.6 for long-document fidelity and nuanced instruction following.</p>
<h3 id="benchmark-comparison-table">Benchmark Comparison Table</h3>
<table>
  <thead>
      <tr>
          <th>Benchmark</th>
          <th>GPT-5.4</th>
          <th>Claude Opus 4.6</th>
          <th>Gemini 3.1 Pro</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>OSWorld (computer use)</td>
          <td><strong>75.0%</strong></td>
          <td>~68%</td>
          <td>~62%</td>
      </tr>
      <tr>
          <td>SWE-bench Pro (coding)</td>
          <td><strong>57.7%</strong></td>
          <td>~56%</td>
          <td>~53%</td>
      </tr>
      <tr>
          <td>GDPval (knowledge work)</td>
          <td><strong>83.0%</strong></td>
          <td>~76%</td>
          <td>~74%</td>
      </tr>
      <tr>
          <td>Human baseline (OSWorld)</td>
          <td>72.4%</td>
          <td>—</td>
          <td>—</td>
      </tr>
  </tbody>
</table>
<p>Note: Claude Opus 4.6 and Gemini 3.1 Pro figures are approximations based on published benchmarks as of Q1 2026; official numbers may vary by evaluation methodology.</p>
<h3 id="api-cost-comparison">API Cost Comparison</h3>
<p>At standard pricing, GPT-5.4 ($2.50/M input) is competitively priced against Claude Opus 4.6 ($3.00/M input) and Gemini 3.1 Pro ($2.00/M input). GPT-5.4-pro ($30/M input) is positioned above all standard competitor offerings and targets enterprise use cases where accuracy premium is justifiable.</p>
<h2 id="best-practices-and-production-recommendations">Best Practices and Production Recommendations</h2>
<p>Production-ready GPT-5.4 deployments share several patterns that distinguish robust integrations from fragile prototypes. The most important is <strong>matching reasoning effort to task complexity at the routing layer</strong>: use a lightweight classifier (even gpt-5.4-mini at <code>none</code> effort) to route incoming requests to the appropriate effort level before forwarding to gpt-5.4. This alone typically reduces per-request cost by 30–50% without measurable quality loss at the application level. Second, <strong>implement structured output validation</strong> for all tool-calling workflows — gpt-5.4 is highly reliable but not infallible, and downstream systems should validate JSON structure before acting on tool call arguments. Third, <strong>monitor long-context billing proactively</strong>: instrument token counts before each request and alert when a conversation history approaches the 272K threshold so you can summarize or truncate before incurring the 2x surcharge. Finally, for computer use agents, implement <strong>screenshot diffing</strong> to detect when an action had no visible effect — this prevents infinite loops where the model keeps clicking a button that isn&rsquo;t registering.</p>
<h3 id="production-checklist">Production Checklist</h3>
<ul>
<li>Set <code>reasoning.effort</code> explicitly — don&rsquo;t rely on defaults for cost-sensitive workloads</li>
<li>Use <code>stream: true</code> for all requests with <code>high</code> or <code>xhigh</code> effort to improve perceived latency</li>
<li>Track <code>usage.total_tokens</code> per response and alert above 272K input</li>
<li>Validate tool call JSON with a schema before executing side effects</li>
<li>For computer use: implement action timeout + screenshot diff to detect stuck states</li>
<li>Cache frequent system prompts with the Prompt Caching API to reduce input token costs</li>
<li>Use <code>gpt-5.4-mini</code> for pre-filtering and routing, gpt-5.4 for execution</li>
</ul>
<h3 id="migration-from-gpt-4o-in-3-steps">Migration from gpt-4o in 3 Steps</h3>
<ol>
<li>Update <code>model=&quot;gpt-4o&quot;</code> to <code>model=&quot;gpt-5.4&quot;</code> in all API calls</li>
<li>Add <code>reasoning={&quot;effort&quot;: &quot;medium&quot;}</code> as a default; tune per endpoint based on latency requirements</li>
<li>Test with your existing prompt suite — gpt-5.4 is backward-compatible but often returns longer, more detailed responses at <code>medium</code>+ effort, which may affect downstream parsers</li>
</ol>
<hr>
<h2 id="faq">FAQ</h2>
<p><strong>What is GPT-5.4 and when was it released?</strong></p>
<p>GPT-5.4 is OpenAI&rsquo;s most capable general-purpose model as of 2026, released on March 5, 2026. It combines a 1,050,000-token context window, five reasoning effort levels (<code>none</code> through <code>xhigh</code>), native computer use, and full tool support in a single Chat Completions API model.</p>
<p><strong>How do I access the GPT-5.4 API?</strong></p>
<p>Access GPT-5.4 through the standard OpenAI Chat Completions API by setting <code>model=&quot;gpt-5.4&quot;</code>. It requires an OpenAI API key with access to GPT-5.4 (available to all paid API tiers as of Q2 2026). No endpoint changes or new authentication methods are needed.</p>
<p><strong>What is the <code>reasoning.effort</code> parameter and which level should I use?</strong></p>
<p><code>reasoning.effort</code> controls how much internal chain-of-thought reasoning the model performs. Use <code>low</code> or <code>none</code> for classification and simple extraction, <code>medium</code> for most conversational and summarization tasks, and <code>high</code> or <code>xhigh</code> for complex code synthesis, agentic planning, or high-stakes analysis. xhigh costs 3–5x more than low but delivers measurably better accuracy on complex tasks.</p>
<p><strong>How much does GPT-5.4 cost compared to GPT-5.4-pro?</strong></p>
<p>Standard gpt-5.4 costs $2.50 per million input tokens and $15 per million output tokens. GPT-5.4-pro costs $30/$180 per million input/output tokens — 12x more expensive but optimized for accuracy-critical enterprise workloads. Input tokens beyond 272K are billed at 2x the standard rate for both variants.</p>
<p><strong>Can GPT-5.4 replace Selenium or Playwright for browser automation?</strong></p>
<p>For exploratory, exception-heavy, or visually complex automation tasks, GPT-5.4&rsquo;s computer use (75% OSWorld accuracy) is a strong alternative to selector-based automation. For high-volume, stable UI workflows where selectors are reliable, traditional automation remains faster and cheaper. Most teams use GPT-5.4 computer use for the hard cases — onboarding flows, exception handling, testing dynamic UIs — while keeping Playwright for predictable, high-frequency tasks.</p>
]]></content:encoded></item></channel></rss>