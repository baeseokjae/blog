<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Configuration on RockB</title><link>https://baeseokjae.github.io/tags/configuration/</link><description>Recent content in Configuration on RockB</description><image><title>RockB</title><url>https://baeseokjae.github.io/images/og-default.png</url><link>https://baeseokjae.github.io/images/og-default.png</link></image><generator>Hugo</generator><language>en-us</language><lastBuildDate>Thu, 23 Apr 2026 01:10:34 +0000</lastBuildDate><atom:link href="https://baeseokjae.github.io/tags/configuration/index.xml" rel="self" type="application/rss+xml"/><item><title>CLAUDE.md Setup Guide 2026: The Config File That Makes Claude Code Actually Useful</title><link>https://baeseokjae.github.io/posts/claude-md-setup-guide-2026/</link><pubDate>Thu, 23 Apr 2026 01:10:34 +0000</pubDate><guid>https://baeseokjae.github.io/posts/claude-md-setup-guide-2026/</guid><description>Complete CLAUDE.md setup guide for 2026: structure, examples, hierarchy, hooks, and best practices to maximize Claude Code in any project.</description><content:encoded><![CDATA[<p>CLAUDE.md is the project instructions file that Claude Code reads before every session — it&rsquo;s the single most impactful configuration you can make. Drop it in your repo root, add your coding conventions and architecture notes, and Claude stops asking the same questions every session.</p>
<hr>
<h2 id="what-is-claudemd-the-system-prompt-for-your-codebase">What Is CLAUDE.md? The System Prompt for Your Codebase</h2>
<p>CLAUDE.md is a Markdown file that acts as a persistent system prompt scoped to your project. Unlike conversation-level instructions that disappear after compaction, CLAUDE.md is re-read from disk at the start of every session and after every context compaction event. Introduced by Anthropic in August 2025, the format caught on fast enough that competitors shipped their own versions — GEMINI.md, .cursorrules, AGENTS.md — within months. By early 2026, 71% of developers who regularly use AI agents were using Claude Code (Pragmatic Engineer Survey, 15,000 developers), and the CLAUDE.md pattern had become the de facto standard for project-level AI configuration.</p>
<p>The core purpose is simple: tell Claude what it needs to know about <em>your specific project</em> that it cannot infer from reading the source files. This includes architectural decisions, conventions that deviate from common defaults, commands to run, things Claude should never do, and project-specific context. You write CLAUDE.md; Claude reads it. The relationship between CLAUDE.md and Claude&rsquo;s auto memory (MEMORY.md) is complementary — you own CLAUDE.md, Claude owns auto memory, and both persist across sessions through different mechanisms.</p>
<p>The practical payoff is immediate. With a well-structured CLAUDE.md, Claude stops generating code that violates your conventions, stops asking what test runner you use, and stops proposing architectures you&rsquo;ve already decided against. That improvement compounds across hundreds of sessions.</p>
<hr>
<h2 id="how-claude-code-discovers-and-loads-claudemd-files">How Claude Code Discovers and Loads CLAUDE.md Files</h2>
<p>Claude Code uses a layered discovery system that loads multiple CLAUDE.md files simultaneously, merging their instructions in priority order. Understanding this discovery order is essential for monorepos and large projects where different directories need different rules.</p>
<p>The load order from highest to lowest priority:</p>
<ol>
<li><strong>Managed policy</strong> — Enterprise-level policy set by your org admin (cannot be overridden)</li>
<li><strong>User global</strong> — <code>~/.claude/CLAUDE.md</code> — applies to every project on your machine</li>
<li><strong>Project root</strong> — <code>./CLAUDE.md</code> — the standard project-level file</li>
<li><strong>Local project</strong> — <code>./.claude/CLAUDE.md</code> — gitignore-friendly variant for personal overrides</li>
<li><strong>Parent directories</strong> — Any CLAUDE.md files in directories above your working directory</li>
<li><strong>Subdirectory files</strong> — <code>./src/CLAUDE.md</code>, <code>./packages/api/CLAUDE.md</code>, etc.</li>
</ol>
<p>The critical distinction for monorepos: <strong>parent-directory CLAUDE.md files load at launch</strong>, while <strong>subdirectory files load on demand</strong> when Claude accesses that directory. This means your root CLAUDE.md is always in context, but a <code>packages/api/CLAUDE.md</code> only gets loaded when Claude is actually working in that package. This keeps the context window lean when you have many packages.</p>
<p>Claude Code also reads <code>.claude/rules/</code> files on demand based on path matching, and respects an <code>@import</code> syntax for inline file inclusion.</p>
<hr>
<h2 id="the-claudemd-hierarchy-global-project-local-and-subdirectory-files">The CLAUDE.md Hierarchy: Global, Project, Local, and Subdirectory Files</h2>
<p>Managing multiple CLAUDE.md files lets you separate concerns cleanly across teams, machines, and project components. The hierarchy is designed so that personal preferences don&rsquo;t pollute shared project config, and project-wide rules don&rsquo;t need to be repeated in every subdirectory.</p>
<p><strong>User global (<code>~/.claude/CLAUDE.md</code>)</strong> — Personal preferences that apply everywhere: your preferred diff format, how you like code explained, your timezone for date calculations. Keep this short — under 30 lines. It applies to every project, so anything project-specific here is noise.</p>
<p><strong>Project root (<code>./CLAUDE.md</code>)</strong> — The main file checked into version control. Contains architecture decisions, tech stack, shared commands, coding conventions, and constraints the whole team agrees on. This is the file most developers mean when they say &ldquo;CLAUDE.md.&rdquo;</p>
<p><strong>Local project (<code>./.claude/CLAUDE.md</code>)</strong> — Typically gitignored. Good for personal overrides: machine-specific paths, draft notes about what you&rsquo;re currently working on, or instructions you want for your sessions but don&rsquo;t want to enforce on the whole team.</p>
<p><strong>Subdirectory files</strong> — Load on demand. Ideal for monorepos where <code>packages/payments/</code> has different conventions than <code>packages/dashboard/</code>. The subdirectory file adds to (not replaces) the root file.</p>
<p>For most solo projects, you only need the project root file. For teams, add global for personal preferences and consider <code>.claude/rules/</code> for path-scoped automation.</p>
<hr>
<h2 id="the-clauderules-directory-path-scoped-rules-for-large-projects">The .claude/rules/ Directory: Path-Scoped Rules for Large Projects</h2>
<p>The <code>.claude/rules/</code> directory is a folder-based extension to CLAUDE.md that lets you create rule files activating only when Claude is working with specific file paths. Each file in <code>.claude/rules/</code> uses YAML frontmatter with a <code>paths</code> key — a list of glob patterns — to declare its scope. When Claude opens or edits a file that matches any listed pattern, it loads that rules file automatically. This is more surgical than subdirectory CLAUDE.md files: you can apply different rules to <code>*.test.ts</code> files versus <code>src/api/**</code> versus <code>infrastructure/**</code> without restructuring your directory tree or creating nested CLAUDE.md files. In a large codebase where the backend, frontend, infrastructure, and test suites have genuinely different conventions, <code>.claude/rules/</code> prevents the root CLAUDE.md from growing into an unmaintainable wall of conditional statements. Rules files load on demand at pattern-match time, not at startup, so they don&rsquo;t inflate the initial context load. The practical limit is the same as CLAUDE.md — keep each rules file focused, under 100 lines, targeting conventions specific to that path pattern.</p>
<p>Rules files use YAML frontmatter to declare when they apply:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-markdown" data-lang="markdown"><span style="display:flex;"><span>---
</span></span><span style="display:flex;"><span>paths:
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">-</span> &#34;src/api/**&#34;
</span></span><span style="display:flex;"><span>  <span style="color:#66d9ef">-</span> &#34;packages/server/**&#34;
</span></span><span style="display:flex;"><span>description: &#34;Server-side rules: no browser APIs, always use structured logging&#34;
</span></span><span style="display:flex;"><span>---
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Server Rules
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Never use <span style="color:#e6db74">`window`</span>, <span style="color:#e6db74">`document`</span>, or <span style="color:#e6db74">`localStorage`</span> — server-side code only
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Use <span style="color:#e6db74">`logger.info()`</span> not <span style="color:#e6db74">`console.log()`</span> — structured logging required
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> All async functions must have explicit error handling with typed catch blocks
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Database calls go through the repository layer in <span style="color:#e6db74">`src/db/repositories/`</span>, never raw queries in route handlers
</span></span></code></pre></div><p>Rules files load on demand, not at startup. Claude checks path matches when it opens or edits a file and pulls in the matching rules. This keeps startup context lean while ensuring rules are present when relevant.</p>
<p>Key difference from <code>.claude/CLAUDE.md</code>: rules files target specific file patterns and are better for language- or layer-specific conventions. Use CLAUDE.md for project-wide context; use rules files for &ldquo;only when working in X.&rdquo;</p>
<hr>
<h2 id="the-import-syntax-referencing-external-files-in-claudemd">The @import Syntax: Referencing External Files in CLAUDE.md</h2>
<p>The <code>@import</code> syntax is a CLAUDE.md directive that expands an external file inline — Claude reads the referenced file&rsquo;s contents as if they were written directly at that point in CLAUDE.md. This lets you keep the CLAUDE.md itself short and navigable while pulling in detailed reference material that would otherwise inflate the file beyond the 200-line limit. The most common use cases are database schemas (too long to write out, but Claude needs to know the column names), API style guides (often 50–100 lines of endpoint conventions), and architecture decision records that explain why certain patterns are mandated. When Claude reads a CLAUDE.md containing <code>@import</code> directives, it fetches and expands each one before processing the full instruction set — the imported content is fully visible and subject to the same instruction budget as anything else in the file. The import chain supports up to 5 hops of recursion: an imported file can import another, that file can import a third, and so on up to 5 levels deep. Beyond that depth, imports are ignored. Practical constraint: files imported via <code>@import</code> must live on the local filesystem. There is no support for URLs or remote resources. For frequently-updated reference material, <code>@import</code> is better than copy-pasting because you update the source file and CLAUDE.md automatically reflects the change.</p>
<p>Here&rsquo;s how to structure a CLAUDE.md that uses imports:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-markdown" data-lang="markdown"><span style="display:flex;"><span># Project Overview
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>This is a Next.js e-commerce platform for B2B customers.
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Architecture
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>
</span></span><span style="display:flex;"><span>@import docs/architecture/overview.md
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## API Conventions
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>
</span></span><span style="display:flex;"><span>@import docs/conventions/api-style-guide.md
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Database Schema
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>
</span></span><span style="display:flex;"><span>@import docs/schema/current-schema.sql
</span></span></code></pre></div><p>When Claude reads the CLAUDE.md, it expands each <code>@import</code> inline, as if the content were written directly in the file. This is useful for keeping CLAUDE.md itself under the recommended length while still giving Claude access to detailed reference material.</p>
<p>Constraints to know:</p>
<ul>
<li>Maximum 5-hop recursion depth (imported files can import others, up to 5 levels deep)</li>
<li>Each imported file counts toward the total instruction budget</li>
<li>The 200K token context window (1M in beta) is generous, but deep import chains in large docs can add up</li>
</ul>
<p>Practical use: <code>@import</code> is ideal for database schemas, API contracts, and detailed architecture docs that are too long for CLAUDE.md but too important to leave out. Avoid importing files that change frequently without pruning old versions — stale imported content causes stale behavior.</p>
<hr>
<h2 id="7-essential-sections-of-an-effective-claudemd">7 Essential Sections of an Effective CLAUDE.md</h2>
<p>A well-structured CLAUDE.md covers seven areas that address the most common sources of friction between developers and AI coding assistants. Not every project needs all seven — a solo weekend project might need only three — but most production codebases with more than one developer benefit from each section. The order below is intentional: Claude reads CLAUDE.md top to bottom, and earlier context shapes how it interprets later sections. A project overview at the top tells Claude what domain it&rsquo;s working in before it sees any conventions; that framing makes the conventions more parseable. Stack information before commands prevents Claude from suggesting commands that don&rsquo;t match your actual setup. Negative instructions work best at the end, where they override any positive patterns Claude might have developed from reading the earlier sections. The most common CLAUDE.md mistake is omitting the &ldquo;What NOT to Do&rdquo; section — it&rsquo;s the highest-value section in most production files because it directly encodes lessons from real mistakes. Start with the first four sections for immediate improvement; add the remaining three as you discover where Claude consistently goes wrong in your codebase.</p>
<h3 id="1-project-overview-510-lines">1. Project Overview (5–10 lines)</h3>
<p>What the project does, who uses it, and its current stage. Include the production URL if there is one. This orients Claude before it reads a single line of code.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-markdown" data-lang="markdown"><span style="display:flex;"><span><span style="color:#75715e">## Project Overview
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>
</span></span><span style="display:flex;"><span>InventoryOS is a B2B inventory management SaaS for mid-market retailers.
</span></span><span style="display:flex;"><span>Live at app.inventoryos.io, 400+ paying customers.
</span></span><span style="display:flex;"><span>Next.js 15 frontend, FastAPI backend, PostgreSQL + Redis.
</span></span><span style="display:flex;"><span>Currently: migrating from REST to tRPC for the internal API layer.
</span></span></code></pre></div><h3 id="2-technical-stack-and-versions">2. Technical Stack and Versions</h3>
<p>Exact versions matter. Claude behaves differently for React 18 vs React 19, Python 3.11 vs 3.12. Don&rsquo;t assume it will infer from package.json.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-markdown" data-lang="markdown"><span style="display:flex;"><span><span style="color:#75715e">## Stack
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Frontend: Next.js 15.2, React 19, TypeScript 5.4, Tailwind 4.0
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Backend: Python 3.12, FastAPI 0.115, SQLAlchemy 2.0 (async)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> DB: PostgreSQL 16 (primary), Redis 7 (sessions + cache)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Testing: pytest + httpx (backend), Vitest + Testing Library (frontend)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Infra: AWS ECS Fargate, RDS, ElastiCache
</span></span></code></pre></div><h3 id="3-common-commands">3. Common Commands</h3>
<p>The commands Claude should run — and the ones it should never run. This section alone saves enormous back-and-forth.</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-markdown" data-lang="markdown"><span style="display:flex;"><span><span style="color:#75715e">## Commands
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>
</span></span><span style="display:flex;"><span>Development:
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`pnpm dev`</span> — start frontend (port 3000)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`uvicorn app.main:app --reload`</span> — start backend (port 8000)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`docker compose up db redis`</span> — start DB + cache
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>Testing:
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`pytest -x`</span> — run backend tests (stop on first failure)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`pnpm test`</span> — run frontend tests
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`pnpm test:e2e`</span> — Playwright E2E tests (requires dev server running)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>NEVER run:
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`DROP TABLE`</span> or <span style="color:#e6db74">`DELETE FROM`</span> without explicit user confirmation
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`git push --force`</span> under any circumstances
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`pnpm build`</span> in development — it overwrites dev configs
</span></span></code></pre></div><h3 id="4-code-style-and-conventions">4. Code Style and Conventions</h3>
<p>Project-specific patterns that deviate from defaults. Don&rsquo;t list things Claude already knows — focus on your choices.</p>
<h3 id="5-architecture-and-file-organization">5. Architecture and File Organization</h3>
<p>Where things live and why. Especially important for monorepos or projects with non-obvious structure.</p>
<h3 id="6-what-not-to-do">6. What NOT to Do</h3>
<p>This is the most underused and highest-value section. Negative instructions are often more effective than positive ones because they prevent the specific mistakes that have already cost your team time.</p>
<h3 id="7-testing-expectations">7. Testing Expectations</h3>
<p>What level of test coverage you expect, what testing patterns you use, and what Claude should or shouldn&rsquo;t generate when writing tests.</p>
<hr>
<h2 id="real-world-claudemd-examples">Real-World CLAUDE.md Examples</h2>
<p>Real CLAUDE.md files from production projects follow a clear pattern: they are shorter than you&rsquo;d expect, they are blunt, and they spend more words on what not to do than on abstract principles. The examples below cover the four most common project archetypes — minimal solo project, monorepo, Python ML pipeline, and Next.js app. Each one is opinionated by design: the goal is to show how specificity outperforms generality. A CLAUDE.md that says &ldquo;follow TypeScript best practices&rdquo; does nothing; one that says &ldquo;never use <code>any</code>, never write class components, always use Server Components by default&rdquo; gives Claude actionable constraints. Open-source projects provide the best real-world reference points: the Next.js repository (138K GitHub stars), LangChain (128K stars), Excalidraw (118K stars), and Deno (106K stars) all maintain CLAUDE.md files. Their files average 80–150 lines, weight heavily toward commands and constraints, and include specific negative instructions drawn from real contributor mistakes. These examples are simplified but reflect that structure. Adapt the one closest to your project type as a starting template, then prune any line that doesn&rsquo;t reflect an actual decision your team has made.</p>
<h3 id="minimal-project-solo-dev">Minimal Project (Solo Dev)</h3>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-markdown" data-lang="markdown"><span style="display:flex;"><span># CLAUDE.md
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Project
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>Personal finance tracker. Next.js 15 + Prisma + PostgreSQL. Solo project, no need for PR process.
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Commands
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`pnpm dev`</span> — dev server (port 3000)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`pnpm db:push`</span> — push schema changes (dev only)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`pnpm test`</span> — vitest
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Conventions
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">-</span> Functional components only, no class components
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Server Components by default; use &#39;use client&#39; only when state/effects needed
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Prisma for all DB access — no raw SQL
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> tailwind-merge for className joins
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Never
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">-</span> Add comments to obvious code
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Use <span style="color:#e6db74">`any`</span> in TypeScript
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Create new API routes — we use Server Actions
</span></span></code></pre></div><h3 id="monorepo-setup">Monorepo Setup</h3>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-markdown" data-lang="markdown"><span style="display:flex;"><span># CLAUDE.md — Root (applies to all packages)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Repo Structure
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>Turborepo monorepo. Packages: <span style="color:#e6db74">`apps/web`</span> (Next.js), <span style="color:#e6db74">`apps/api`</span> (FastAPI), <span style="color:#e6db74">`packages/shared`</span> (TS types).
</span></span><span style="display:flex;"><span>See per-package CLAUDE.md for package-specific rules.
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Shared Commands
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`turbo dev`</span> — start all services
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`turbo test`</span> — run all tests
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`turbo build`</span> — build all packages
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Shared Conventions
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">-</span> All public APIs must be typed in <span style="color:#e6db74">`packages/shared/types/`</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Breaking changes to shared types require updating all consumers before merge
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Use pnpm workspaces — never install to root package.json
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Never
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">-</span> Import from <span style="color:#e6db74">`apps/`</span> in <span style="color:#e6db74">`packages/`</span> — packages must not depend on apps
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Commit .env files — use .env.example with documented keys only
</span></span></code></pre></div><h3 id="python-ml-project">Python ML Project</h3>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-markdown" data-lang="markdown"><span style="display:flex;"><span># CLAUDE.md
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Project
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>ML pipeline for churn prediction. Trains weekly on Databricks, serves via FastAPI.
</span></span><span style="display:flex;"><span>Python 3.12. Data in S3, models in MLflow.
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Commands
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`make train`</span> — local training run with sample data (50K rows, not full dataset)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`make serve`</span> — start inference API
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`pytest tests/ -v`</span> — run tests
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Data Conventions
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">-</span> Never load full dataset locally — use <span style="color:#e6db74">`data/sample/`</span> for dev/test
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> All features go through <span style="color:#e6db74">`src/features/`</span> pipeline — no ad-hoc transformations in notebooks
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Model artifacts saved to <span style="color:#e6db74">`models/`</span> locally, <span style="color:#e6db74">`s3://ml-models/churn/`</span> in production
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Never
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#66d9ef">-</span> Hard-code thresholds — use config in <span style="color:#e6db74">`conf/model.yaml`</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Commit model artifacts or datasets to git
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> Run experiments in production MLflow tracking server — use staging
</span></span></code></pre></div><hr>
<h2 id="the-200-line-rule-why-shorter-claudemd-files-outperform-longer-ones">The 200-Line Rule: Why Shorter CLAUDE.md Files Outperform Longer Ones</h2>
<p>The 200-line limit for CLAUDE.md is not about token budgets — it&rsquo;s about instruction budgets. Frontier LLMs like Claude reliably follow approximately 150–200 distinct instructions in a given context. Claude Code&rsquo;s own system prompt uses roughly 50 of those slots, leaving around 100–150 instructions for your CLAUDE.md. A 500-line CLAUDE.md likely contains far more than 150 instructions, and the excess dilutes the ones that matter.</p>
<p>The math is straightforward: 100 lines of CLAUDE.md use roughly 500–1000 tokens, a trivial fraction of Claude&rsquo;s 200K token context window. The bottleneck is not tokens — it&rsquo;s attention. Instructions compete for Claude&rsquo;s attention, and in a crowded prompt, low-priority instructions get deprioritized or ignored. A focused 80-line CLAUDE.md where every line earns its place will consistently outperform a sprawling 400-line file full of things Claude already knows.</p>
<p>Pruning strategy: remove anything Claude can infer from the codebase (it reads your files), anything generic that applies to any project (follow REST conventions, write clean code), and anything that hasn&rsquo;t influenced a real interaction in the past month. What remains should be specific, non-obvious, and project-critical. If you&rsquo;re unsure whether a line belongs, delete it and see if Claude&rsquo;s behavior degrades. If it doesn&rsquo;t, the line wasn&rsquo;t doing work.</p>
<p>The 200-line recommendation also applies to auto memory files. The MEMORY.md index is always loaded, but only the first 200 lines are guaranteed to be visible — another reason to keep your index tight.</p>
<hr>
<h2 id="advisory-vs-deterministic-claudemd-instructions-vs-hooks">Advisory vs Deterministic: CLAUDE.md Instructions vs Hooks</h2>
<p>CLAUDE.md instructions are advisory — Claude reads them and tries to follow them, but there is no enforcement mechanism. For conventions like naming patterns, architecture preferences, or explanation style, advisory is fine. For compliance requirements, security rules, or anything that must never be violated, you need hooks.</p>
<p>Hooks are shell commands configured in <code>.claude/settings.json</code> that execute deterministically in response to Claude&rsquo;s actions. They run outside of Claude&rsquo;s context — they cannot be talked out of running, overridden by conversation, or forgotten after compaction.</p>
<p>Common hook patterns:</p>
<p><strong>PostToolUse — run linter after every file edit:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;hooks&#34;</span>: {
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;PostToolUse&#34;</span>: [
</span></span><span style="display:flex;"><span>      {
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;matcher&#34;</span>: <span style="color:#e6db74">&#34;Edit|Write&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;hooks&#34;</span>: [
</span></span><span style="display:flex;"><span>          { <span style="color:#f92672">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;command&#34;</span>, <span style="color:#f92672">&#34;command&#34;</span>: <span style="color:#e6db74">&#34;cd $CLAUDE_PROJECT_ROOT &amp;&amp; pnpm lint --fix $CLAUDE_TOOL_INPUT_FILE_PATH&#34;</span> }
</span></span><span style="display:flex;"><span>        ]
</span></span><span style="display:flex;"><span>      }
</span></span><span style="display:flex;"><span>    ]
</span></span><span style="display:flex;"><span>  }
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p><strong>PreToolUse — block dangerous commands:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-json" data-lang="json"><span style="display:flex;"><span>{
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">&#34;hooks&#34;</span>: {
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">&#34;PreToolUse&#34;</span>: [
</span></span><span style="display:flex;"><span>      {
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;matcher&#34;</span>: <span style="color:#e6db74">&#34;Bash&#34;</span>,
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">&#34;hooks&#34;</span>: [
</span></span><span style="display:flex;"><span>          { <span style="color:#f92672">&#34;type&#34;</span>: <span style="color:#e6db74">&#34;command&#34;</span>, <span style="color:#f92672">&#34;command&#34;</span>: <span style="color:#e6db74">&#34;echo $CLAUDE_TOOL_INPUT_COMMAND | grep -q &#39;DROP TABLE\\|DELETE FROM&#39; &amp;&amp; exit 1 || exit 0&#34;</span> }
</span></span><span style="display:flex;"><span>        ]
</span></span><span style="display:flex;"><span>      }
</span></span><span style="display:flex;"><span>    ]
</span></span><span style="display:flex;"><span>  }
</span></span><span style="display:flex;"><span>}
</span></span></code></pre></div><p>The practical rule: use CLAUDE.md for things you want Claude to consider; use hooks for things that must always happen. A linter running on every edit is a hook. &ldquo;Prefer functional components&rdquo; is a CLAUDE.md instruction.</p>
<hr>
<h2 id="auto-memory-vs-claudemd-complementary-context-systems">Auto Memory vs CLAUDE.md: Complementary Context Systems</h2>
<p>CLAUDE.md and auto memory (the <code>~/.claude/projects/.../memory/</code> system) serve different purposes and complement each other. The distinction is ownership and scope.</p>
<p><strong>You write CLAUDE.md.</strong> It contains project conventions, architecture decisions, commands, and constraints that the whole project depends on. It&rsquo;s checked into version control (usually). Team members benefit from it. It changes when the project changes.</p>
<p><strong>Claude writes auto memory.</strong> It accumulates facts about you — your role, preferences, feedback patterns, recurring decisions — across conversations. It&rsquo;s personal to your Claude Code installation, not shared with teammates. It helps Claude behave consistently with your preferences without you repeating yourself each session.</p>
<p>Auto memory is loaded into every conversation context, but the MEMORY.md index is capped at 200 lines. Individual memory files are read on demand when relevant. CLAUDE.md is always fully loaded.</p>
<p>A common mistake: putting personal preferences in CLAUDE.md and project facts in auto memory. Personal preferences (how you like code explained, your timezone, your preferred diff format) belong in <code>~/.claude/CLAUDE.md</code> or auto memory. Project facts (architecture decisions, team conventions, deployment procedures) belong in the project CLAUDE.md.</p>
<hr>
<h2 id="how-claudemd-survives-context-compaction">How CLAUDE.md Survives Context Compaction</h2>
<p>Context compaction is Claude Code&rsquo;s mechanism for handling long conversations — when the context window fills up, prior messages are summarized to make room. Conversation-level instructions (&ldquo;for this session, always use async/await&rdquo;) do not survive compaction; they get summarized or dropped.</p>
<p>CLAUDE.md survives compaction because it is re-read from disk, not stored in conversation history. After compaction, Claude re-reads your CLAUDE.md as part of restoring its working context. Your project conventions, architecture notes, and command references are all back, even after a long multi-hour session.</p>
<p>This is a meaningful advantage over in-conversation instructions. Teams that rely on repeating instructions at the start of each session or after long gaps can instead encode those instructions in CLAUDE.md once and rely on compaction recovery. The file is your persistent anchor.</p>
<p>The corollary: anything that must persist across sessions should be in CLAUDE.md (or auto memory), not in the conversation. Don&rsquo;t tell Claude your preferred error handling pattern at the start of every session — write it down.</p>
<hr>
<h2 id="claudemd-vs-agentsmd-vs-cursorrules-vs-geminimd">CLAUDE.md vs AGENTS.md vs .cursorrules vs GEMINI.md</h2>
<p>All four formats serve the same basic purpose — project-level AI coding instructions — but each targets a different tool. Here&rsquo;s the comparison:</p>
<table>
  <thead>
      <tr>
          <th>Format</th>
          <th>Tool</th>
          <th>Discovery</th>
          <th>Scope</th>
          <th>Limits</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>CLAUDE.md</td>
          <td>Claude Code</td>
          <td>Hierarchical, multi-file, on-demand subdirs</td>
          <td>Project + global + subdirectory</td>
          <td>~200 lines effective; 200K token window</td>
      </tr>
      <tr>
          <td>AGENTS.md</td>
          <td>OpenAI Codex / GPT-4o agents</td>
          <td>Single file at repo root</td>
          <td>Project-wide only</td>
          <td>Varies by model</td>
      </tr>
      <tr>
          <td>.cursorrules / .mdc</td>
          <td>Cursor IDE</td>
          <td>Single file or <code>.cursor/rules/*.mdc</code> with globs</td>
          <td>Project or path-scoped</td>
          <td>500 lines recommended</td>
      </tr>
      <tr>
          <td>GEMINI.md</td>
          <td>Gemini Code Assist</td>
          <td>Single file at repo root</td>
          <td>Project-wide</td>
          <td>Varies</td>
      </tr>
      <tr>
          <td>.github/copilot-instructions.md</td>
          <td>GitHub Copilot</td>
          <td>Single file</td>
          <td>Repository-wide</td>
          <td>No stated limit</td>
      </tr>
  </tbody>
</table>
<p>The formats are similar enough that a well-written CLAUDE.md can serve as the source of truth, with minimal adaptation needed for other tools. The main differences are discovery hierarchy (Claude&rsquo;s multi-file layered approach is the most flexible) and path-scoping (Claude&rsquo;s <code>.claude/rules/</code> and Cursor&rsquo;s <code>.cursor/rules/*.mdc</code> are equivalent; others lack this).</p>
<p>If your team uses multiple AI tools, maintain CLAUDE.md as the canonical source and keep AGENTS.md / .cursorrules as lightweight references to it, or adapt the content as needed. Don&rsquo;t maintain independent versions — they&rsquo;ll drift.</p>
<hr>
<h2 id="common-mistakes-what-not-to-put-in-claudemd">Common Mistakes: What NOT to Put in CLAUDE.md</h2>
<p>Understanding what to leave out is as important as knowing what to include. These are the most common CLAUDE.md mistakes that degrade Claude&rsquo;s performance.</p>
<p><strong>Duplicating what Claude already knows.</strong> &ldquo;Follow REST conventions,&rdquo; &ldquo;write clean code,&rdquo; &ldquo;use meaningful variable names&rdquo; — Claude knows these. Restating them wastes instruction budget. Only include things specific to your project or your deviations from common practice.</p>
<p><strong>Style enforcement without hooks.</strong> If you need ESLint to run on every save, put it in a PostToolUse hook. Putting &ldquo;always run ESLint after editing&rdquo; in CLAUDE.md means Claude will try to remember to run it — sometimes it will, sometimes it won&rsquo;t. Hooks are deterministic; instructions are not.</p>
<p><strong>Growing without pruning.</strong> CLAUDE.md tends to accumulate. Every team member adds their favorite rule; every incident generates a new constraint. Set a calendar reminder to audit your CLAUDE.md quarterly. Remove anything that hasn&rsquo;t changed Claude&rsquo;s behavior in the past month. A shorter, more focused file is always better.</p>
<p><strong>Secrets and credentials.</strong> Never put API keys, passwords, or environment variable values in CLAUDE.md. It&rsquo;s committed to version control (usually), it&rsquo;s in Claude&rsquo;s context window, and it may be logged or transmitted. Secrets go in <code>.env</code> files (gitignored) or your secrets manager.</p>
<p><strong>Conflicting instructions across files.</strong> If your root CLAUDE.md says &ldquo;use tabs&rdquo; and your <code>packages/frontend/CLAUDE.md</code> says &ldquo;use spaces,&rdquo; Claude will apply whichever it read most recently in the current context. Audit your full CLAUDE.md hierarchy periodically to catch conflicts.</p>
<p><strong>Instructions that Claude ignores.</strong> After making changes to CLAUDE.md, test the new instructions immediately. Ask Claude to describe its understanding of the convention you added, then have it write some code and verify it follows the rule. If Claude isn&rsquo;t following an instruction, it&rsquo;s usually too vague or buried in a long file.</p>
<hr>
<h2 id="quick-start-your-first-claudemd-in-5-minutes">Quick Start: Your First CLAUDE.md in 5 Minutes</h2>
<p>The minimum viable CLAUDE.md takes under five minutes to write and produces a noticeable improvement immediately. It requires four things: one paragraph describing what the project is and who uses it, the exact commands to start the dev server and run tests, five conventions that are specific to your project (not generic advice), and three things Claude should never do in this codebase. That&rsquo;s it. You don&rsquo;t need a perfect file on day one — the goal is to stop Claude from making the same mistakes it made in your last session. Start with what you had to correct most often last week. The &ldquo;Never&rdquo; section deserves at least half your initial effort: it&rsquo;s where you encode hard-won lessons. Every time you catch Claude doing something wrong, ask yourself whether that behavior would be prevented by a well-worded negative instruction. If yes, add it. The file grows organically from real friction. The template below gives you a skeleton; filling in the brackets honestly — based on your actual project, not an idealized version — is the only requirement. A specific CLAUDE.md that&rsquo;s occasionally incomplete is more effective than a generic one that tries to cover everything.</p>
<p>Here&rsquo;s a starter template:</p>
<p>Here&rsquo;s a starter template:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-markdown" data-lang="markdown"><span style="display:flex;"><span># CLAUDE.md
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Project
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>
</span></span><span style="display:flex;"><span>[One paragraph: what the project does, who uses it, what stage it&#39;s in, and the main URL if live]
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Stack
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> [Language + version]
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> [Framework + version]
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> [Database + version]
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> [Test runner]
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Commands
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`[dev command]`</span> — start dev server
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`[test command]`</span> — run tests
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> <span style="color:#e6db74">`[build command]`</span> — build for production
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>NEVER run: [list commands that are dangerous in this repo]
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Conventions
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> [Convention 1 — something specific to your project, not generic advice]
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> [Convention 2]
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> [Convention 3]
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> [Convention 4]
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> [Convention 5]
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e">## Never
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> [Thing Claude keeps doing wrong that you don&#39;t want]
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> [Architectural pattern to avoid]
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">-</span> [Security or compliance constraint]
</span></span></code></pre></div><p>Fill in the brackets honestly. The &ldquo;Never&rdquo; section especially benefits from real examples — think about the last three times Claude did something you had to correct, and put those corrections in writing. Real constraints beat abstract rules every time.</p>
<p>After saving the file, start a new Claude Code session and ask Claude to describe the project and its conventions. If Claude&rsquo;s description matches what you wrote, the file is working. If it misses something important, that section needs to be more explicit.</p>
<hr>
<h2 id="faq">FAQ</h2>
<p>These are the questions developers ask most often when setting up CLAUDE.md for the first time. They cover cross-surface compatibility, multi-file behavior, instruction verification, documentation imports, and version control strategy — the practical decisions you&rsquo;ll face in the first week of using CLAUDE.md seriously. If your question isn&rsquo;t covered here, the fastest path to an answer is to test it: create a short CLAUDE.md with a specific, testable instruction (&ldquo;always add a type annotation to every function parameter&rdquo;), start a fresh Claude Code session, and verify that Claude&rsquo;s code output reflects the instruction. That test-and-verify loop is the most reliable way to calibrate your CLAUDE.md against Claude&rsquo;s actual behavior, since the effective instruction budget varies by project complexity, context window usage, and how deeply Claude is reasoning about the current task. The five questions below represent the most common setup challenges and decision points.</p>
<h3 id="does-claudemd-work-with-claude-code-in-vs-code-and-jetbrains-or-only-in-the-terminal">Does CLAUDE.md work with Claude Code in VS Code and JetBrains, or only in the terminal?</h3>
<p>CLAUDE.md works across all Claude Code surfaces — the terminal CLI, the VS Code extension, the JetBrains extension, and the web app at claude.ai/code. All surfaces use the same discovery and loading logic. The file you create for terminal use works automatically in IDE extensions.</p>
<h3 id="what-happens-if-i-have-claudemd-in-both-my-home-directory-and-my-project-root">What happens if I have CLAUDE.md in both my home directory and my project root?</h3>
<p>Both files load. The global <code>~/.claude/CLAUDE.md</code> applies first, then the project root CLAUDE.md adds to it. Instructions don&rsquo;t conflict unless they&rsquo;re directly contradictory — Claude merges them. If there is a conflict, the more specific (project-level) instruction generally takes precedence. Keep the global file short (personal preferences only) and let project files handle project specifics.</p>
<h3 id="how-do-i-know-if-my-claudemd-instructions-are-actually-being-followed">How do I know if my CLAUDE.md instructions are actually being followed?</h3>
<p>The fastest test: start a fresh Claude Code session (to ensure no cached state), then ask Claude directly: &ldquo;Describe this project&rsquo;s conventions and what you should never do.&rdquo; Compare Claude&rsquo;s answer to your CLAUDE.md. Then have it write a short piece of code and check whether the conventions appear. Instructions that Claude doesn&rsquo;t follow are usually too vague or buried in a long file.</p>
<h3 id="can-i-use-claudemd-to-give-claude-access-to-private-documentation-or-internal-wikis">Can I use CLAUDE.md to give Claude access to private documentation or internal wikis?</h3>
<p>Sort of. You can use <code>@import</code> to include file contents inline, but CLAUDE.md can only reference files on the local filesystem. You can&rsquo;t reference URLs directly. For internal docs, export the relevant sections to Markdown files and import them. For large reference docs, prefer path-scoped rules in <code>.claude/rules/</code> so they load only when relevant.</p>
<h3 id="should-i-commit-claudemd-to-git-or-keep-it-private">Should I commit CLAUDE.md to git, or keep it private?</h3>
<p>Commit the project root CLAUDE.md to version control — it&rsquo;s most useful when the whole team benefits from it. For personal overrides (your machine-specific paths, draft notes, instructions you want but your team doesn&rsquo;t), use <code>.claude/CLAUDE.md</code> with a gitignore entry. The split between shared project config (tracked) and personal overrides (ignored) is the recommended pattern for teams.</p>
]]></content:encoded></item></channel></rss>